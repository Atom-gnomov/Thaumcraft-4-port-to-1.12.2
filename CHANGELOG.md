# Changelog

Все заметные изменения порта Thaumcraft 4 → 1.12.2 фиксируются здесь.
Каждый набор исправлений = новая версия. Номер версии задаётся в `mod/build.gradle`
(`version = "x.y.z"`); собранный jar называется `Thaumcraft-x.y.z-universal.jar`.

Процесс выпуска новой версии:
1. Поднять `version` в `mod/build.gradle` (patch — багфиксы, minor — новый контент).
2. Добавить запись в этот файл.
3. Собрать: `JAVA_HOME=.../jdk8u492-b09 ./gradlew.bat build -x test --console=plain`.
4. Задеплоить `mod/build/libs/Thaumcraft-x.y.z-universal.jar` в `.minecraft/mods/`.
5. Закоммитить и запушить.

## [1.0.15]
### Исправлено
- **Топор Стихий: рубил только один блок без всех фич.** `BlockUtils.breakFurthestBlock`
  был заглушкой («сломай этот блок»). Восстановлен оригинальный алгоритм валки:
  жадный обход связанных брёвен (куб ±2 за шаг, лимиты ±24/±48/±24), ломается
  самый дальний — дерево валится сверху вниз; вокруг сломанного бревна
  планируются random-тики (листва распадается). Дропы летят к игроку:
  восстановлен полноценный `EntityFollowingItem` (был заглушкой) с трейлом
  частиц; топор вызывает валку с followItem=true/color=10 как в оригинале.
- **Жезл при удержании ПКМ «перенажимался» каждый тик; после отпускания звук и
  трата вис продолжались.** Вис хранится в NBT и тратится каждый тик, а ванила
  сравнивает активный стек по NBT — использование сбрасывалось и перезапускалось
  ежетикно. Добавлен `canContinueUsing` (сравнение только по предмету).
- **Груды камней в Волшебном лесу — из замшелого булыжника** (в оригинале
  `field_150341_Y` = mossy cobblestone, порт ставил обычный камень).
- **Имена боссов при любом способе спавна.** Страж и Голем Древних теперь
  вызывают `makeChampion` в `onInitialSpawn` (как Претор) — имя форматируется
  и для яиц/summon, а не только для спавнов через замки боссов.
- **Радиальное меню фокусов**: убран кламп курсора (порт-добавка, дёргал и
  прятал курсор, мешал выбору); текст тултипа рисуется с тенью и явным
  z-смещением над плашкой (`drawStringWithShadow`, z=301) — плашка больше не
  должна быть пустой.
- **Тултипы жезлов приведены к оригиналу**: строка «Ёмкость N (avg % затрат)»,
  компактная сводка вис по стихиям (Shift — развёрнуто с процентом потребления
  и стоимостью каста фокуса), имя фокуса зелёным (Shift — его апгрейды).
  Добавлены ключи `tc.vis.cost`/`tc.vis.costavg` в оба языка.

## [1.1.1.0]
### Смена схемы версий + Магнит мобов (реестр расхождений, п. 2)
Первый релиз в новом формате `A.B.C.D` — см. «Нумерация версий» ниже.

- **Магнит мобов** — вариант магнита из `meta & 2`, ровно как в оригинале:
  один блок, второй бит метаданных выбирает `TileMobMagnet` вместо
  `TileMagnet`. Наследует всю механику предметного магнита (работа только под
  редстоуном, радиус `сигнал / 2`, скорость 0.25, переключение
  притягивание ⇄ отталкивание), но двигает живых существ. Игроков не трогает
  никогда.
- **Переключатель «взрослые / детёныши»** повторяет флаг `adult` оригинала:
  у существ с возрастом берётся та половина, что выбрана.
- **Форма душ** (`ItemSoulMould`) — ПКМ по существу запоминает его вид,
  sneak-ПКМ в воздухе очищает. Положенная в слот магнита, сужает захват до
  одного вида существ (в оригинале — `ItemSoulMould.getPatternName`).
- **Экран** (`ContainerMobMagnet` + `GuiMobMagnet`) на текстуре оригинала:
  слот формы душ и радиопара «взрослые/детёныши», кнопки через
  `enchantItem`/`sendEnchantPacket`. У предметного магнита клик по-прежнему
  переключает режим — экран только у мобьего, как в оригинале.
- `TileMagnet` обобщён двумя хуками (`getTargetClass`, `isTarget`) — это
  наш аналог переопределяемого `getEntitySelector()` оригинала.
- Рецепты: магнит мобов из предметного + гнилая плоть, форма душ из
  таумия и глины. Blockstate расширен до четырёх вариантов
  (притяжение/отталкивание × предметы/мобы), в креативе оба магнита.

## [1.1.2.0]
### KAMI — фундамент эндгейм-тира Thaumic Tinkerer
Начат перенос KAMI: в оригинале это 57 файлов и ~6600 строк (инструменты,
броня, варп-гейт, портал бедрока, свои фокусы, ихоровый кошель, обработчики
душ и брони). Этот заход кладёт основание, на котором стоит всё остальное.

- **Ресурсы KAMI** (`ItemKamiResource`, 8 подтипов) — порядок подтипов взят из
  `LibItemNames.KAMI_RESOURCE_NAMES` оригинала и является несущим: по этим
  метам адресуются и рецепты, и дроп. Ихор, ихоровая ткань, ихориевый слиток,
  самородок, набалдашник, стержень из ихоровой ткани, осколок Ада, осколок
  Края. Все — `EnumRarity.EPIC`, как в оригинале.
- **Дроп осколков** (`DimensionalShardDropHandler`) — точные шансы оригинала:
  эндермен в Крае даёт осколок Края с шансом 1/32, зомби-свин в Аду —
  осколок Ада с шансом 1/16; только при убийстве игроком и только в своём
  измерении. Это единственный источник осколков — они не крафтятся.
- **Рецепты — узор в узор и аспект в аспект** из `ItemKamiResource.getRecipeItem`:
  ихор — инфузия на незеритовой звезде с нестабильностью 7
  (MAN 32 + LIGHT 32 + SOUL 64, компоненты: алмаз, 8 осколков Края, око
  эндера, 8 осколков Ада, выход 8); ихоровая ткань — аркан по 125 каждого
  первичного (зачарованная ткань + ихор + алмазы, выход 3); ихориевый слиток и
  набалдашник — по 100 каждого первичного; стержень — инфузия с
  нестабильностью 9 (MAGIC 100 + LIGHT 32 + TOOL 32); самородок — 9 из слитка.
- Текстуры перенесены из ассетов TT, локализация en + ru, guard пинит порядок
  подтипов, редкость, шансы дропа, ключевые аспекты рецептов и регистрацию.

### Осталось по KAMI (следующие заходы)
Инструменты (ихоровые кирка/топор/лопата/меч + продвинутые, режимы и HUD),
броня (ихоровая ткань + самоцветная, обработчик и модель крыльев), варп-гейт
(блок, тайл, два экрана, рендер), портал бедрока, фокусы (Отзыв, Теневой луч,
Вытяжка опыта), ихоровый кошель, талисман блоков, небесная жемчужина,
протоглина, наконечник и стержень для жезла, обработчик душевых сердец.
Плюс ветка исследований KAMI — она же закрывает п. 6 и 12 реестра.

## [1.1.3.0]
### KAMI — детали жезла и ихориевые инструменты
- **Ихориевый набалдашник и стержень из ихоровой ткани** — теперь это детали
  жезла, а не просто ресурсы: набалдашник `ICHOR` со скидкой вис 0.8 и
  стоимостью 10, стержень `ICHORCLOTH` ёмкостью 1000, стоимостью 10 и свечением
  — значения оригинала. Ресурсы из 1.1.2.0 перестали быть бесполезными: они и
  есть эти детали.
- **Материал ICHOR** перенесён числом в число:
  `addToolMaterial("ICHOR", 4, -1, 10F, 5F, 25)`. Прочность `-1` — не описка
  оригинала, а суть тира: инструменты не изнашиваются.
- **Четыре ихориевых инструмента** (кирка, топор, лопата, меч) на этом
  материале, уровень добычи 4, редкость EPIC. Рецепты — узор в узор из
  `getRecipeItem` каждого: все на великодревном стержне из ихориевых слитков,
  по 75 одного первичного аспекта (кирка — FIRE, топор — WATER, лопата —
  EARTH, меч — AIR).
- Текстуры перенесены из ассетов TT, локализация en + ru, guard пинит числа
  материала, редкость, уровень добычи, параметры деталей жезла и регистрацию.

### Осталось по KAMI
Продвинутые инструменты (`*Adv` — режимы и HUD), броня (ихоровая ткань и
самоцветная, обработчик, крылья), варп-гейт (блок, тайл, два экрана, рендер),
портал бедрока, три фокуса (Отзыв, Теневой луч, Вытяжка опыта), ихоровый
кошель, талисман блоков, небесная жемчужина, протоглина, обработчик душевых
сердец, ветка исследований KAMI.

## [1.1.4.0]
### KAMI — измерение бедрока
Зависимость продвинутых инструментов: они ломают бедрок и открывают проход
сюда, поэтому измерение переносится первым.

- **Мир целиком из бедрока**, все 256 слоёв — как в оригинале, где он строился
  из `new FlatLayerInfo(256, Blocks.bedrock)`. Ни неба под ногами, ни пещер:
  всё ценное спрятано внутри монолита.
- **Рудные кластеры** (`OreClusterGenerator`) — числа оригинала: `density`
  кластеров на чанк, каждый делает 200 попыток заложить жилу до 20 блоков на
  высоте 6…250, вырезая её **в бедроке**. Генерируются только в этом измерении.
- **Таблица частот руд** перенесена целиком, вес в вес (уголь 2648, железо
  1503, алмаз 67, Ordo 31, винтеум 392 и т.д.), вместе с чёрным списком
  (`oreFirestone`). Записи — имена из ore dictionary, поэтому подхватываются
  руды других модов, если они стоят; в оригинале так же.
- **Портал бедрока** — появляется там, где продвинутый инструмент проломил
  бедрок. При входе переносит в измерение и выжигает карман на высотах
  251–253, чтобы игрок не оказался внутри монолита (ровно как в оригинале).
- Измерение и его генератор зарегистрированы; id и плотность руды вынесены в
  конфиг (`bedrock_dim`, `bedrock_ore_density`).

**Замечание по сверке:** хук столкновения с порталом сначала был написан как
`onEntityCollidedWithBlock` по памяти и не скомпилировался. Правильное имя в
маппингах этой сборки — `onEntityCollision`; проверено по `forgeBin` через
`javap`, а не подобрано. Без него портал бы не срабатывал: коллизии у блока
нет, игрок сквозь него падает, и `onEntityWalk` не вызывается.

### Осталось по KAMI
Продвинутые инструменты (теперь их зависимость закрыта), броня, варп-гейт,
три фокуса, ихоровый кошель, талисман блоков, небесная жемчужина, протоглина,
обработчик душевых сердец, ветка исследований KAMI.

## Нумерация версий

С **1.1.1.0** формат четырёхсоставной — `A.B.C.D`, где каждая следующая цифра
менее значима: **A** — веха порта, **B** — направление работ (`0` — корпус
TC4-парити и adoption FOREVA, `1` — модуль Thaumic Tinkerer), **C** — фича или
заход внутри направления, **D** — доводка уже выпущенной фичи (багфикс, ассеты,
локализация). Плоская нумерация закончилась на `1.0.59` и остаётся историей;
работы по корпусу продолжатся с `1.0.60.0`.

## [1.1.11.0]
### Второй проход аудита: четыре числа разошлись + Шлем открытия

Проверка уже перенесённого на выдумки, теперь по числам, а не по рецептам.
Таблицы аспектов сошлись полностью — все 14 базовых стоимостей своих
зачарований TT и все 29 ванильных/таумкрафтовых совпадают аспект в аспект,
как и уровни, слоты и типы всех четырнадцати. Разошлись четыре вещи:

1. **Кривая стоимости зачарования округляла, а оригинал отбрасывает.**
   `MiscHelper.multiplyAspectList` делает `(int)(значение * множитель)`, а у нас
   стоял `Math.round`. На первом уровне множитель 1.2, поэтому база 4 стоила
   **5 вместо 4**, база 8 — 10 вместо 9, и так далее почти везде. Заодно убран
   `Math.max(1, ...)`: в оригинале его нет и он не нужен.
2. **Радиус магнита считался в дробных блоках.** В оригинале
   `int redstone / 2` — целочисленное деление, сигнал 7 даёт **3 блока, не 3.5**.
   (Текст исследования в оригинале обещает 7.5 — код обещает 7. Работает код.)
3. **Верх области притяжения магнита был на полблока выше.** У оригинала
   `y + 0.5 + range`, у нас было `y + range + 1.0`.
4. **Свои зачарования регистрировались с `Rarity.RARE`.** Оригинал передаёт
   вес **0** — их нельзя выбить случайно. Минимум, выразимый в 1.12.2, это
   `VERY_RARE` (вес 1); стол зачарований и так закрыт через
   `canApplyAtEnchantingTable`.

**Отдельно стоит отметить:** guard-тест на радиус магнита требовал именно
`redstone / 2.0D` — то есть закреплял ошибку, а не оригинал. Тест, написанный
по порту вместо исходника, цементирует ровно тот баг, который должен ловить.
Assertion переписан, в нём оставлен комментарий об этом.

### Шлем открытия
`ItemRevealingHelm` — таумиевый шлем со встроенными гогглами: показывает узлы и
всплывающие подсказки, даёт те же 5% скидки на вис, защищает как тауминий,
чинится слитками тауминия, 500 прочности. Аркан-рецепт `"GH"` — гогглы рядом со
шлемом, по 5 каждого примала. Текстуры предмета и модели брони из оригинала.

Guard — 39 тестов, зелёные. Полный набор: 583 теста, те же 5 предсуществующих
падений клиентского рендера. Очередь: **48 из 84**.

## [1.1.10.0]
### Тринадцать рецептов блоков переписаны с оригинала + Ткань поглощения заклинаний

Последний кусок долга из аудита 1.1.5.0. Рецепты фокусов и утилит поправили в
1.1.6.0, а блоки так и остались выдуманными — все тринадцать.

**Пять из тринадцати вообще не аркан-крафты:**

| Объект | Было у нас | В оригинале |
| --- | --- | --- |
| Закоптившийся кварц (блок, колонна, резной, плита, ступени) | аркан за `ARCANESTONE` | **обычный верстак**: 4 самоцвета → блок, 2 блока → 2 колонны, 2 плиты → резной, 3 блока → 6 плит, 6 блоков → 4 ступени (и зеркальный вариант) |
| Матрица души | аркан | **тигель**: жемчуг Эндера + BESTIA 4 / COGNITIO 8 / SENSUS 8 |
| Ткань поглощения заклинаний | не было | **тигель**: зачарованная ткань + MAGIC 10 / PERDITIO 6 / PERMUTATIO 4 |
| Осмотический стол зачарований | аркан | **инфузия 15** на столе зачарований: 5 обсидиановых тотемов, 2 тауминия, ткань поглощения |
| Таум-восстановитель | аркан | **инфузия 8** на блоке тауминия: железо, золото, алмаз, булыжник, доски, кожа, амбра, тауминий |

Остальные восемь — аркан, но с другими формами и компонентами:

- **Воронка** — одна строка `STS` (камень, тауминий, камень), ORDO 1 / PERDITIO 1.
  Было 3×3 из железа и ванильной воронки.
- **Магнит** — `" I "/"SIs"/"WFW"`: магический лог по краям, **фокус телекинеза**
  в центре низа, осколки воздуха и земли. Было «железо вокруг редстоуна».
- **Магнит мобов** — та же форма, но золото вместо железа (с откатом на
  `ingotCopper` через ore dictionary, как в оригинале). **Выход — предметный
  урон 1**, а не 2: это заодно чинит баг, который я внёс в 1.1.5.0, сменив
  нумерацию магнита и не поправив рецепт. Крафт выдавал предмет без модели и
  без имени.
- **Трансвектор** — `"BRB"/"LEL"/"BRB"` с лазуритом и жемчугом Эндера.
- **Соединитель** — `" I "/" WI"/"S  "` (железо, палка, осколок порядка), ORDO 2.
- **Дислокатор** — столбик `" M "/" I "/" C "`: зеркальное стекло, трансвектор,
  компаратор.
- **Динамическая дощечка** — `"GIG"/"ICI"` с **чистым ядром голема** (мета 100).

### Ткань поглощения заклинаний
Понадобилась чарователю, поэтому портирована здесь же. 35 использований,
кладётся в сетку крафта рядом с зачарованным предметом и снимает с него все
чары; сама остаётся в сетке и изнашивается на единицу. Перенесён и
`INoRemoveEnchant` — пустой интерфейс-хук, которым мод помечает предмет,
у которого чары снимать нельзя.

Оговорка порта: у `Item` в 1.12.2 больше нет
`doesContainerItemLeaveCraftingGrid`, которым оригинал удерживал ткань в
сетке. Теперь это решает сам рецепт — `SpellClothRecipe.getRemainingItems`.

Guard — 36 тестов, зелёные. Полный набор: 580 тестов, те же 5 предсуществующих
падений клиентского рендера. Очередь: **47 из 84**.

## [1.1.9.0]
### Гиперэнергетический нитор и шесть пропитанных огней

Один предмет открывает семь объектов, поэтому взят вперёд номера в очереди.

- **Гиперэнергетический нитор** (`ItemBrightNitor`) — обычный нитор,
  передержанный в тигле: ENERGY 25 / LIGHT 25 / AIR 10 / FIRE 10. Сам по себе
  ничего не делает, из него делаются все шесть огней.
- **Шесть пропитанных огней** — каждый тоже тигель на одном ниторе:
  Aer и Aqua и Terra по FIRE 5 / MAGIC 5 / свой примал, Ordo и Perditio так же,
  а **Ignis выбивается** — у него нет MAGIC и вдвое больше FIRE (FIRE 10 / AIR 5).
  Так в оригинале.

Огонь живёт, только пока рядом есть блок, который он умеет переделывать; иначе
гаснет, и гаснет от дождя. Каждый со своей таблицей превращений, перенесённой
пара в пару:

| Огонь | Что делает |
| --- | --- |
| **Aer** | сушит: брёвна и земля → песок, листва → песчаник, лёд → стекло. Вода → **торт** (шутка оригинала, оставлена) |
| **Aqua** | морозит: песок и адский песок → лёд, адский камень → снег, лава → обсидиан |
| **Terra** | возвращает к почве: камень, дерево, стекло, булыжник → земля; адский кирпич → доски; **спавнер → блок железа** |
| **Ignis** | тянет Нижний мир: трава и земля → адский камень, песок → адский песок, любая руда → кварцевая, посевы → адский нарост, лава → вода |
| **Ordo** | доводит руду до блока металла. Сверх ванильной девятки проходит по ore dictionary и парит каждый `oreFoo` с `blockFoo` — модовые руды тоже, как в оригинале, с кешем. Срабатывает **один раз из трёх**, остальные сгорают |
| **Perditio** | съедает остальные пять, оставляя обычный огонь. Тикает **каждый тик**, а не раз в 200 |

Два бага оригинала перенесены сознательно и подписаны в коде:

- `setBlockWithTransmutationTarget` принимал `(x, y, z)`, а читал
  `getBlock(x, z, y)` — последние два переставлены. Один из двух вызывающих
  передавал их уже переставленными, и ошибка взаимно гасилась, второй — нет.
  Оба вызова передают ровно то же, что и в оригинале, поэтому поведение
  совпадает на обеих ветках.
- `getChanceOfNeighborsEncouragingFire` сведён к «да/нет»: воздух с подходящим
  соседом даёт 100, всё остальное 0, тогда как ваниль взвешивает соседей.

Текстуры (включая анимацию `.mcmeta`) взяты из ассетов оригинала; модели
повторяют ванильные модели огня с подменённой текстурой — 10 моделей и
multipart-блокстейт на каждый огонь.

Guard — 34 теста, зелёные. Очередь: **46 из 84**.

## [1.1.8.0]
### Кольцо чёрной дыры — последний заблокированный рецепт закрыт

Следующий объект из очереди: сам ни от чего не зависит, а от него зависело
единственное, что ещё не регистрировалось.

- **Кольцо чёрной дыры** (`ItemBlockTalisman`, KAMI) — бабл-кольцо, которое
  привязывается к блоку по ПКМ и хранит его без ограничения по количеству.
  Скрытый ПКМ включает и выключает (урон предмета 0/1, как в оригинале).
  Надетым и включённым сметает все стопки своего блока из инвентаря в себя,
  оставляя одну пополненной; когда в инвентаре ни одной — выдаёт стопку 64.
  ПКМ по контейнеру наполняет контейнер из кольца. Правило выбора «выжившей»
  стопки перенесено дословно, вместе с его оговоркой «только за пределами
  хотбара» (`highest > 8`). Оригинал разговаривал с инвентарями через
  `ISidedInventory` — здесь это capability предметов, разговор тот же.
- **Инфузия по оригиналу**: нестабильность 9, VOID 65 / DARKNESS 32 /
  MAGIC 50 / ELDRITCH 32, на фокусе портативной дыры, с ихором, эндер-сундуком,
  алмазом, вторым ихором, слизью порчи и пустотной банкой.
- **Терраформинговое стекло разблокировано** — инфузия 12, CRAFT 65 /
  CRYSTAL 32 / MAGIC 50 / MIND 32 на самом кольце. С 1.1.6.0 оно не
  регистрировало ничего, потому что компонента не было; теперь **у всех
  предметов и блоков модуля настоящий рецепт оригинала**.
- **Два состояния иконки** у кольца и обоих талисманов. Все три переключаются
  уроном предмета 0/1, и оригинал регистрирует по иконке на состояние; в порт
  была перенесена только «выключенная». Обе выкачаны из ассетов оригинала.

### Ещё два предмета того же тира
- **Бездонная сумка** (`ItemIchorPouch`) — сумка для набалдашников, выросшая до
  **13 × 9 = 117 слотов** и больше не ограниченная набалдашниками: держит что
  угодно кроме другой сумки, по 64 в слоте. Носится на поясе. Экран на текстуре
  оригинала 256×256, сетка `(12 + x*18, 8 + y*18)`, инвентарь игрока с (48, 177)
  — числа оригинала. Слот с самой открытой сумкой заблокирован, и перенесён
  запрет горячей клавиши для него: без него сумку можно подменить из-под
  собственного экрана и задублировать (issue 367 в оригинале).
  Найдено попутно: сумка наследует `ItemFocusPouch`, поэтому обычный экран
  сумки радостно принимал её и сохранял только первые 18 слотов из 117 —
  остальное пропадало. Обычный экран теперь её не берёт.
- **Протоглина** (`ItemProtoclay`) — лежит в инвентаре и смотрит, по какому
  блоку сейчас ударит пробуждённый инструмент в руке; если для этого блока в
  инвентаре есть более подходящий пробуждённый инструмент, меняет их местами.
  Камень — кирка, земля — лопата, дерево — топор; меч не трогает. Для этого
  добавлен `IAdvancedTool` оригинала, и три пробуждённых инструмента теперь
  объявляют свой тип через него, а не строкой в подсказке.
- Экран сумки получил id **50** — как в `LibGuiIDs` оригинала, где KAMI-экраны
  начинаются с 50 (дальше 51 и 52 под небесный портал).

### Замечание к очереди
`TT_PORT_QUEUE.md` видит только зависимости **по рецепту**. Поведенческие она
не видит: `ItemSkyPearl` числится свободным, но настраивается исключительно
кликом по небесному порталу, которого нет, — портировать его сейчас значит
получить мёртвый предмет. Предупреждение вписано в шапку генератора.

### Инфраструктура и долги
- **Генераторы документации положены в репозиторий** — `scripts/gen_tt_queue.py`
  и `scripts/gen_tt_reference.py` на общей `scripts/tt_common.py`. Оба файла
  раньше заявляли, что сгенерированы, но скриптов не было, так что проверить
  это было нельзя. Заодно починены две ошибки прежней генерации: объект больше
  не значится зависимостью самого себя (в рецепте оригинала выход записан как
  `new ItemStack(this)`), и учтены переименования при портировании — из-за них
  `ItemConnector` числился неперенесённым, хотя он у нас
  `ItemTransvectorConnector`. Счёт стал честным: **37 из 84**.
- ~~**`mod/gradle.properties` с абсолютным путём чужой машины**~~ — закрыто.
  Строка `org.gradle.java.home` убрана из версионируемого файла; в нём осталась
  инструкция, как указать свой JDK 8 (`-Dorg.gradle.java.home=<путь>` либо
  `~/.gradle/gradle.properties`). Раньше сборка падала у всех, кроме одной
  машины.
- **Абсолютные пути вычищены из документации** — везде теперь относительные от
  корня репозитория (`../tt-original-1.7.10`, `../tools/jdk8u492-b09`), потому
  что работа идёт с нескольких машин.

Guard — 30 тестов, зелёные. Полный набор: 574 теста, те же 5 предсуществующих
падений клиентского рендера.

## [1.1.7.0]
### Закоптившийся кварц (самоцвет) — и оба талисмана разблокированы
Первый объект из новой очереди `TT_PORT_QUEUE.md`, выбранный по зависимостям:
самоцвет ни от чего не зависит и открывает сразу два рецепта.
- **Закоптившийся кварц** (`ItemDarkQuartz`) — предмет-самоцвет, из которого
  в оригинале делаются блоки и оба талисмана. Крафтится **по 8 штук** из
  кольца кварца вокруг угля; оригинал регистрировал рецепт дважды — с
  обычным и с древесным углём, — обе версии перенесены.
- **Талисман Remedium** и **Талисман Withhold** больше не заблокированы:
  зарегистрированы их настоящие инфузии (5 и 6 нестабильности) с
  аспектами и компонентами оригинала, включая 4 и 1 самоцвет соответственно.
- Верстачные рецепты TT регистрируются из `ConfigTinkerer` через событие
  реестра рецептов — вне аудируемого TC4-корпуса.
- Заблокированным остаётся только **Worldshaper's Looking Glass** (нужен
  блок-талисман); его рецепт выписан в javadoc.
- `TT_PORT_QUEUE.md` перегенерирован: портировано 35 из 82.

## [1.1.6.0]
### Рецепты фокусов и утилит переписаны по оригиналу (правило «не выдумывать»)
Все рецепты в `ConfigTinkerer` для моих объектов были подобраны «по аналогии»
и аудит 1.1.5.0 отметил их как выдуманные. Переписаны дословно из
`getRecipeItem()` оригинала (через `TT_OBJECT_REFERENCE.md` и исходник).
- **Семь фокусов**: пять — инфузия, два — аркан, с настоящими компонентами:
  Переплавка `FNE` (фокус огня + нитор + фокус раскопок, FIRE 10/ORDER 5/
  ENTROPY 6); Телекинез (инфузия 5, жемчуг + 4 кварца + железо + золото +
  осколок воздуха); Полёт (инфузия 3, жемчуг + 4 кварца + 2 пера + осколок);
  Лечение (инфузия 4, фокус печа + золотая морковь + 3 самородка);
  Отражение (инфузия 5, **на фокусе полёта** + 2 зеркальных стекла +
  укрепляющий камень + осколок порядка); Дислокация (инфузия 8, жемчуг +
  4 кварца + 3 амбры + алмаз); Эндер-сундук (аркан, столбец
  зеркало/око/фокус дыры).
- **Кошачий амулет**: инфузия 8 на кварцевом блоке (ихор + 2 золота +
  какао-бобы + тропические листья + рыба), DARKNESS 16/ORDER 32/MIND 16.
- **Ничего не зарегистрировано** для трёх, которым нужны отсутствующие
  предметы — их рецепты и блокеры выписаны в javadoc `ConfigTinkerer`:
  талисманы Remedium и Withhold ждут **самоцвет** тёмного кварца (у нас есть
  только блок), Worldshaper's Looking Glass — **блок-талисман**.
- Ключи исследований — оригинальные (`FOCUS_SMELT` и т. д.). Ветка
  исследований TT ещё не портирована, поэтому рецепты зарегистрированы, но
  заперты — это гейт оригинала, а не подмена.
- Guard переписан: пинит рецепт амулета и **отсутствие** трёх выдуманных.

## [1.1.5.0]
### Оригинал скачан локально, каталог объектов, сверка перенесённого

Заход целиком про то, чтобы больше ничего не выдумывать.

- **Исходник Thaumic Tinkerer 1.7.10 лежит локально и постоянно** —
  `../tt-original-1.7.10` рядом с репозиторием (ветка `1.7.10`, 301 файл в
  `thaumic/tinkerer`). Туда же вынесен JDK 8 сборки: `../tools/jdk8u492-b09`.
- **`TT_OBJECT_REFERENCE.md`** — все **84** регистрируемых объекта оригинала
  выписаны дословно: рецепт (аркан/инфузия/тигель/верстак), аспекты,
  нестабильность, конструктор с твёрдостью и габаритами, запись исследования с
  координатами и родителями, константы. Извлечено скриптом из `getRecipeItem()`
  и `getResearchItem()`, не пересказано.
- **Правило «ничего не выдумывать» записано в руководство для агентов**
  (`PORTING_HANDOFF.md`, раздел «THE RULE: never invent a value»): сначала
  каталог, потом исходник, потом транскрипция; названия — только из
  `en_US.lang`/`ru_RU.lang` самого оригинала; баги оригинала тоже переносятся;
  если 1-в-1 заблокирован — не регистрируем ничего и пишем блокер в реестр.

### Исправлено по итогам сверки
- **Константы блоков.** Тёмный кварц (сопротивление 6→10), воронка (`IRON`→
  `ROCK`, звук металла→камня), магнит (3.0/8.0→**1.7/1.0**, звук→дерево),
  восстановитель (3.0/8.0→**5.0/10.0**), чарователь (3.0/12.0→**5.0/2000.0**),
  трансвектор и дислокатор (`ROCK`→`IRON`, 12→10), планшет динамизма
  (`ROCK`→`IRON`, 10→**50**, звук→металл).
- **Габариты.** Воронка, магнит и планшет в оригинале — плиты
  (1/8, 2/16, 2/16 блока), чарователь — 0.75 высоты; у нас все четыре были
  полными кубами. Добавлены `getBoundingBox` с числами оригинала.
- **Магнит мобов — предметный урон 1, а не 2.** Добавлен `BlockMagnetItem`,
  повторяющий `ItemBlockMagnet` оригинала (`getMetadata`: 0→0, иначе→2), и
  `damageDropped` по таблице оригинала. Заодно у варианта появилось своё имя.
- **Названия — из языковых файлов оригинала, а не свои.** Оригинал везёт
  собственные `en_US.lang` и `ru_RU.lang`; всё перенесено оттуда. Магнит — это
  **Kinetic Attractor / Кинетический притяжатель**, а не «Магнит»; воронка —
  **Essentia Funnel**, восстановитель — **Thaumic Restorer**, кошачий амулет —
  **Feline Amulet**, зеркало — **Worldshaper's Looking Glass**, фокус плавки —
  **Wand Focus: Efreet's Flame**, тёмный кварц — **Smokey Quartz**, прокачанные
  инструменты — **Awakened**. Итого 40 строк переписано и 32 добавлено в
  `en_us.lang`, 54 и 32 в `ru_ru.lang`, включая все 14 названий зачарований.
- **Режимы прокачанных инструментов у каждого свои.** Раньше все три делали
  «10-блочный бур». В оригинале третий режим кирки — **Line** (бур), лопаты —
  **Column** (шахта −8…+7, только из того же блока, что ударили), топора —
  **Tree** (валит ствол `breakFurthestBlock`, затем стягивает весь дроп в
  радиусе 5 и на 64 вверх к пню).
- **`KamiToolHandler` переписан как дословная транскрипция `ToolHandler`**,
  вместе с багами оригинала — сравнением абсолютных координат с офсетами цикла
  в `removeBlocksInIteration` и неиспользуемыми `silk`/`fortune`. Оба помечены
  комментарием «так в оригинале, не чинить». Бедрок теперь обрабатывает только
  кирка, как и было.

### Осталось (долг, значения уже в каталоге)
Рецепты в `ConfigTinkerer` всё ещё свои: 7 фокусов, 4 утилитарных предмета и
~13 блоков. Не хватает объектов-предпосылок (самоцвет тёмного кварца,
`ItemSpellCloth`, `ItemBlockTalisman`) — заглушки ставить нельзя, поэтому до их
появления настоящие рецепты не регистрируются. Ветка исследований TT не
портирована. Подробная таблица расхождений — в `THAUMIC_TINKERER_PLAN.md`.

## [1.0.59]
### Кошачий амулет и зеркало установки приведены к оригиналу
Ревизия показала, что и эти два предмета из 1.0.43 были выдуманы. Переписаны
по исходнику.
- **Кошачий амулет** (Baubles, слот амулета) — больше не гасит урон от
  падения. Как в оригинале, он **перепрошивает ИИ мобов** в радиусе 10 (и 4
  по высоте): те, кто убегает от оцелотов, начинают убегать от носителя; те,
  кто охотится на игроков, переключаются на цель, которой в мире нет; крипер
  сбрасывает отсчёт и теряет цель.
- **Зеркало установки** — больше не ставит один блок. Как в оригинале, оно
  выкладывает **целый квадрат** блоков по грани, на которую смотришь,
  оплачивая каждый блок из инвентаря; sneak-ПКМ в воздухе переключает размер
  3×3 → 5×5 → 7×7 → 9×9 → 11×11 → 3×3; привязка (sneak-клик по блоку)
  работает только на обычных полных блоках.
- Локализация en+ru, guard расширен.

## [1.0.58]
### Тёмный кварц: плита и ступени (п. 9 реестра закрыт)
- **Плита из тёмного кварца** (половинная и двойная, `ItemSlab` на обе) с
  твёрдостью и сопротивлением оригинала (0.8 / 10), нижняя/верхняя половины.
- **Ступени из тёмного кварца**, вырезанные из обычного варианта — как
  `BlockDarkQuartzStairs` оригинала.
- Blockstate'ы, модели и локализация en+ru; guard расширен.

## [1.0.57]
### Талисманы стали Baubles (п. 7 реестра закрыт)
Оба талисмана из 1.0.43 работали из инвентаря — в оригинале это Baubles со
своей механикой. Переписаны 1-в-1.
- **Талисман очищения**: носится в слоте амулета, имеет выключатель
  (sneak-ПКМ) и **100 зарядов**. Раз в секунду тушит горение или снимает один
  вредный эффект и тратит на это один заряд.
- **Талисман опыта**: носится в слоте амулета, в режиме поглощения втягивает
  орбы опыта в радиусе **3** до предела **1500**, при заполнении сам
  выключается. ПКМ меняет **10 опыта + стеклянную бутылку** на бутыль опыта;
  sneak-ПКМ переключает поглощение. Тултип показывает запас и режим.
- Локализация тултипов на en+ru, guard расширен.

## [1.0.56]
### Воронка переделана в оригинальное устройство (п. 8 реестра закрыт)
Воронка из 1.0.45 была пылесосом для дропнутых предметов — в оригинале это
совсем другое устройство. Переписана 1-в-1 по `TileFunnel` оригинала.
- Держит в своём слоте **наполненную банку** и каждый тик переливает из неё
  **одну единицу эссенции** в банку, на которую указывает **ванильный хоппер
  под воронкой**.
- Приёмник проверяется как в оригинале: пустая банка принимает, если её
  фильтр пуст или совпадает с аспектом; непустая — если аспект тот же и в ней
  меньше 64 (для пустотной банки предел не действует).
- Тайл — `IAspectContainer` (содержимое = банка в слоте) и отдаёт слот через
  `IItemHandler`, так что банку можно подавать хоппером.
- Банка кладётся и забирается правым кликом по блоку и выпадает при ломании.
- Guard переписан под настоящее поведение.

## [1.0.55]
### Интеграция Thaumic Tinkerer — 14 своих зачарований (п. 1 реестра закрыт)
Перенесены все четырнадцать зачарований оригинала (`ModEnchantments` и
`ModEnchantmentHandler`), формула в формулу.
- **Оружие:** Вампиризм (лечит на уровень за удар), Последний удар (шанс
  1 к 20−уровень на тройной урон), Доблесть (+10% за уровень ниже половины
  здоровья), Сосредоточенные и Рассеянные удары (считают серии ударов по
  одной цели через NBT предмета).
- **Броня:** Толчок ввысь (прыжок ×(уровень+2)/2), Замедленное падение
  (гасит скорость и дистанцию падения), Ударная волна (при падении с >3
  бьёт всё в радиусе 10 на 0.1×уровень×дистанцию), Прыжок хищника (+25% за
  уровень, пока под ногами воздух).
- **Инструменты:** Дробление (×3×уровень на блоках твёрже 20, иначе ×0.8),
  Проходка (запоминает курс, +20% за уровень при отклонении <50°, иначе
  ×0.3), Распад и Переплавка (мгновенно ломают свой тип блока за 1 прочности
  и отказываются ломать остальное — как в оригинале).
- **Лук:** Быстрый натяг (ускоряет натяжение).
- Правила совместимости, максимальные уровни и слоты — оригинала; ни одно не
  выпадает на столе зачарования (только через осмотический чарователь).
  Переплавка по-прежнему не ставится на Топор Стихий.
- Стоимости в чарователе — базовые из `EnchantmentManager` оригинала.
  Названия на en и ru. Guard `TinkererEnchantmentsStaticGuardTest`.

## [1.0.54]
### Ревизия TT и приведение фокусов к оригиналу
Сверка заходов 1.0.42–1.0.45 с исходником `Thaumic-Tinkerer@1.7.10` показала,
что они делались без эталона и почти всё в них разошлось. Найденные пять
расхождений записаны в реестр плана (пп. 5–9); первое из них закрыто здесь.
- **Все 7 фокусов приведены к оригиналу по поведению и цене.**
  Smelt, Telekinesis, Heal и Deflect стали **канальными** (работа и оплата
  каждый тик) вместо мгновенных.
- **Deflect** — снова щит от снарядов: снаряды рядом отбрасываются вдоль
  собственного вектора на удвоенное расстояние, свои снаряды пропускаются.
  Было: ударная волна с отбросом мобов.
- **Dislocation** — снова перенос блока: первый каст поднимает блок вместе с
  NBT его тайл-энтити, второй ставит его к выбранной грани; цена 500/500/100,
  ×5 для блока с тайлом, ×20 для спавнера. Было: телепорт игрока.
- **Smelt** — счётчик прогресса `20 - min(3,потенция)*5` на конкретный блок,
  плавит только блок→блок. **Heal** — полсердца на такте
  `30 - потенция*10/3` с проверкой `shouldHeal()`. **Flight** — импульс
  `1/1.5 * (1 + потенция*0.2)` и сброс счётчика полёта на сервере.
- Стоимости аспект в аспект: Smelt 45🔥+12, Telekinesis 5+5, Flight 15,
  Heal 45+45, Deflect 8+4, EnderChest 100+100, Dislocation 500/500/100.
- Guard расширен: канальность, стоимости и исправленные механики.
- **Не закрыто:** рецепты и исследования фокусов (у оригинала пять из семи —
  инфузия, и своя ветка `KEY_FOCUS_*`), пп. 6–9 реестра.

## [1.0.53]
### Thaumic Tinkerer — настоящий экран чарователя (книжный путь удалён)
- **`ContainerEnchanter` + `GuiEnchanter`** на перенесённой текстуре оригинала
  (`gui/enchanter.png`). Раскладка по `GuiEnchanting` TT: кнопка старта
  (151, 33), сетка предложенных зачарований в две строки по восемь от (34, 54)
  со сдвигом первой строки на −24 при появлении второй, очередь строками за
  правым краем (шаг 26 px) с кнопками «−»/«+» уровня, шесть вертикальных полос
  по первичным аспектам (высота 59, заливка 48) с тултипом «уплачено/всего».
- **Зачарованная книга больше не используется** — временное решение из 1.0.51
  удалено вместе с кодом (`beginFromBook`). Теперь, как в оригинале, ПКМ по
  блоку открывает экран, а зачарования выбираются в нём.
- Очередь редактируется в тайле: `setEnchant` (добавить / сменить уровень /
  убрать), `start()`, пересчёт стоимости после каждой правки. Уровень
  ограничивается максимумом зачарования — дальше таблица стоимости оригинала
  не определена. Во время работы правки запрещены.
- Предложения фильтруются как в оригинале: предмет должен быть зачаровываемым,
  без уже наложенных чар, зачарование должно подходить предмету и не
  конфликтовать с тем, что уже в очереди.
- Слоты инструмента (15, 33) и жезла (15, 57) — через `SlotItemHandler`, в
  жезловый слот принимается только палочка. Кнопки идут через
  `enchantItem`/`sendEnchantPacket`, id-схема повторяет пакеты оригинала:
  0 — старт, 1..16 — предложения, далее по три id на строку очереди.
- Перенесены 29 иконок зачарований из ассетов TT (`textures/enchants/`) —
  по одной на каждое поддерживаемое зачарование.
- Guard переписан под настоящий GUI: наличие контейнера, экрана и текстуры,
  проводка старта/предложений/уровней, редактируемость очереди и фильтры
  предложений.

## [1.0.52]
### Thaumic Tinkerer — настоящий GUI планшета динамизма (вместо упрощения)
Правка курса: в 1.0.50 у планшета вместо экрана оригинала было прямое
взаимодействие (sneak-клики, редстоун в руке). Это было упрощение, а модуль
должен повторять оригинал 1-в-1 — упрощение убрано.
- **`ContainerAnimationTablet` + `GuiAnimationTablet`** на перенесённой
  текстуре оригинала (`gui/animation_tablet.png`). Раскладка по координатам
  оригинала: слот инструмента (80, 15), радиопара «удар»/«применение»
  в (52, 15) и (111, 15), переключатель управления редстоуном по центру на
  y+60, подписи теми же цветом и выравниванием.
- Нажатия кнопок идут через `enchantItem` / `sendEnchantPacket` — тот же
  транспорт, что у остальных GUI этого порта (арканный верстак, манипулятор
  фокусов, исследовательский стол), без собственного пакета.
- **Клик жезлом вращает планшет** и играет `thaumcraft:tool`, как в оригинале;
  всё остальное открывает экран. Прежние sneak-переключатели удалены.
- Слот подключён через `SlotItemHandler`, поэтому capability-инвентарь тайла
  остался единственным источником истины — воронки и трубы работают как раньше.
- Guard переписан: пинит вращение жезлом, открытие GUI, наличие контейнера,
  экрана и текстуры, и передачу обоих переключателей кнопками контейнера.

### Осталось до 1-в-1
- **Чарователю нужен настоящий экран выбора зачарований** (сетка кнопок,
  уровни, тултипы со стоимостью, гейт по исследованиям). Сейчас запуск идёт
  зачарованной книгой — временное решение, помечено в
  `THAUMIC_TINKERER_PLAN.md` как долг и идёт следующим заходом. Механика тайла
  (многоблок, стоимость, расход вис) готова и менять её не потребуется.

## [1.0.51]
### Интеграция Thaumic Tinkerer — Осмотический чарователь (Фаза 3 завершена)
- **Осмотический чарователь** (`BlockEnchanter` + `TileEnchanter`) —
  зачаровывает инструмент, оплачивая работу вис из жезла: по 1 единице за тик,
  как в оригинале. Работает только внутри многоблока оригинала: **шесть**
  столбов в радиусе 4, каждый из 2–12 обсидиановых тотемов с нитором сверху
  (проверка `findPillar` воспроизведена 1-в-1, включая границы 2 и 12).
- **Таблица стоимости портирована из оригинала** (`EnchantmentCosts`, из
  `EnchantmentManager` TT): ручные значения для уровня 1 по каждому
  зачарованию и та же экспоненциальная кривая
  `стоимость × уровень × (1 + уровень × 0.2)` — уровень 1 стоит ×1.2 базы,
  уровень 5 — ×10. Включены 24 ванильных зачарования и все 5 таумкрафтовых
  (Мощь/Бережливость/Спешка/Починка/Клад). Своих зачарований TT
  (ascent boost, slow fall, auto smelt) в порту нет — пропущены.
- Оплачиваются только первичные аспекты: жезлы в TC4 держат лишь их, и
  оригинал по факту тоже проверял и тянул только первичные (у него, например,
  BEAST-часть стоимости Приманки не проверялась вовсе).
- **Вместо GUI оригинала — зачарованная книга.** В оригинале был экран со
  списком всех зачарований; здесь ПКМ книгой ставит её зачарования в очередь и
  применяет их **в полную силу, игнорируя правила совместимости и лимиты
  наковальни** — в чём и смысл устройства. Книга расходуется только при
  успешном старте. ПКМ инструментом/жезлом кладёт их в слоты, пустой рукой —
  забирает, sneak-ПКМ отменяет работу. Слоты открыты через `IItemHandler`.
- Если многоблок разрушить во время работы — процесс останавливается, но
  уплаченный вис не теряется (как в оригинале при провале проверки столбов).
  При ломании блока содержимое слотов выпадает.
- Рецепт за исследованием `INFUSIONENCHANTMENT`. Guard расширен: форма
  многоблока, состав столбов, кривая стоимости, отдельные базовые значения,
  расход вис по 1, только первичные, применение по оплате, расход книги.
- **Фаза 3 закрыта** — планшет динамизма и чарователь.

## [1.0.50]
### Интеграция Thaumic Tinkerer — Планшет динамизма (Фаза 3, часть 1)
- **Планшет динамизма** (`BlockAnimationTablet` + `TileAnimationTablet`) —
  держит один инструмент и работает им по блоку перед собой через
  fake-player. Два режима, как в оригинале:
  - **удар** — атакует существо в клетке перед собой, а если его нет — грызёт
    блок;
  - **применение** — «правый клик» предметом по блоку или существу
    (сначала пробует взаимодействие с существом, затем активацию блока, затем
    `onItemUse`, затем `onItemRightClick` — порядок оригинала).
- Цикл замаха сохранён по оригиналу: {45 градусов, скорость 3} — то есть удар
  раз в ~30 тиков.
- **Ломка прогрессивная, а не мгновенная.** Каждый замах добавляет прочность
  по `getDigSpeed` инструмента, блок разрушается при накоплении, а пока идёт
  работа — рисуется ванильная сетка трещин. Мгновенная ломка была бы сильно
  сильнее оригинала.
- Дропы подтягиваются к планшету, чтобы не разлетались.
- **Вместо GUI оригинала — прямое взаимодействие:** ПКМ кладёт/забирает
  инструмент, sneak-ПКМ переключает режим удар/применение, sneak-ПКМ с
  редстоуном в руке переключает «работать непрерывно» ⇄ «ждать импульса».
  Слот открыт через `IItemHandler` — инструменты можно подавать воронкой.
  При ломании блока инструмент выпадает.
- Планшет не бьёт собственного fake-player'а (иначе он атаковал бы сам себя).
- Рецепт за исследованием `GOLEMANCY`. Guard расширен на всё перечисленное.
- **Известное упрощение:** TESR замаха нет — планшет не показывает
  качающийся инструмент, механика при этом работает полностью.

## [1.0.49]
### Интеграция Thaumic Tinkerer — Трансвекторный дислокатор (Фаза 2 завершена)
- **Трансвекторный дислокатор** (`BlockTransvectorDislocator` +
  `TileTransvectorDislocator`) — по импульсу редстоуна меняет местами блок
  перед собой и связанный блок в радиусе 16 (`DISLOCATOR_DISTANCE`
  оригинала), перенося вместе с ними тайл-энтити и существ, стоящих в обеих
  точках. Срабатывает строго по фронту сигнала; импульс, пришедший во время
  10-тикового отката, запоминается и отрабатывает по его истечении — как в
  оригинале, поэтому быстрый тактовый генератор не «съедает» срабатывания.
- Отказывается двигать то же, что и оригинал: ауровые узлы (`blockAiry`
  мета 0), всё из `portableHoleBlackList` и небьющиеся блоки (прочность −1:
  бедрок, порталы). Воздух двигать можно — так меняются местами блок и пустота.
- Перенос тайлов сделан аккуратно: NBT снимается, тайл убирается из мира
  (чтобы ваниль не инвалидировала переносимый), затем состояния меняются
  местами и тайлы воссоздаются с исправленными координатами.
- Состояние `powered` вынесено в blockstate — включённый дислокатор
  подсвечивается анимированной текстурой оригинала.
- Рецепт за исследованием `MIRROR`. Guard расширен: радиус, откат, запоминание
  импульса, перенос тайлов и существ, все три запрета, срабатывание по фронту.
- **Фаза 2 (трансвекторы) закрыта** — интерфейс, соединитель и дислокатор.

## [1.0.48]
### Интеграция Thaumic Tinkerer — Трансвекторный интерфейс + соединитель
- **Трансвекторный интерфейс** (`BlockTransvectorInterface` +
  `TileTransvectorInterface`) — подменяет собой другой блок в радиусе 4 (как в
  оригинале, `INTERFACE_DISTANCE`): всё, что обращается к интерфейсу за
  инвентарём, баками, энергией или эссенцией, получает ответ от связанного
  блока. Классическая механика TT: сундук за стеной, тигель через пол.
- **Трансвекторный соединитель** (`ItemTransvectorConnector`) — связывание в
  два клика, как в оригинале: ПКМ по устройству «берёт» его, ПКМ по цели —
  связывает. Sneak-ПКМ по устройству разрывает связь. Отказы объясняются
  текстом в статус-строке (не то устройство, слишком далеко, устройство
  исчезло, два интерфейса в цепь нельзя).
- **Делегирование через capability вместо ручных интерфейсов.** Оригинал
  вручную реализовывал `ISidedInventory`, `IFluidHandler`, энергию BuildCraft
  и RF — по классу на каждый мод. В 1.12 всё это ходит через capability, и
  проброс одного `getCapability` покрывает предметы, жидкости и энергию любых
  модов разом, включая те, которых в 1.7.10 не существовало. Интерфейсы
  эссенции Thaumcraft (`IAspectContainer`, `IEssentiaTransport`) — не
  capability, поэтому делегируются явно.
- **Жёстче оригинала по безопасности:** связь в незагруженный чанк возвращает
  «нет цели» вместо принудительной загрузки (чтобы устройство не держало
  территорию и не запускало генерацию из тика), связь сама с собой отсекается,
  вышедшая за радиус — сбрасывается.
- Общая база `TileTransvector` (цель + проверка радиуса по осям + NBT)
  подготовлена под дислокатор из следующего захода. Рецепты за исследованием
  `MIRROR`. Guard расширен на все перечисленные контракты.

## [1.0.47]
### Интеграция Thaumic Tinkerer — Ремонтник (первый блок на эссенции)
- **Ремонтник** (`BlockRepairer` + `TileRepairer`) — держит один повреждённый
  предмет и чинит его, вытягивая эссенцию из трубы со стороны, к которой
  приставлен. Байт-в-байт по балансу оригинала: раз в 10 тиков тянется ровно
  1 единица эссенции, а сколько прочности она вернёт — зависит от аспекта:
  `TOOL` 8, `CRAFT` 5, `ORDER` 3. Блок объявляет тягу `TOOL` силой 128, но
  принимает любой из трёх, так что банка с любым из них его прокормит.
- Направление (`facing`) выставляется при установке в сторону блока, к которому
  ставят, — приложил к трубе, и он подключён. ПКМ меняет предмет внутри на
  тот, что в руке; при ломании блока инструмент выпадает, а не пропадает.
  Слот открыт наружу через `IItemHandler`, поэтому воронки и трубы могут
  подавать инструменты и забирать починенные. В очках ревелятора остаток
  повреждения виден как `ENTROPY` (как в оригинале).
- Ветка Tinkers' Construct из оригинала осознанно не портирована.
- Рецепт за исследованием `DISTILESSENTIA`. Guard расширен: регистрация,
  сохранение оригинальных значений починки, вытягивание ровно 1 эссенции,
  capability слота, выпадение предмета при ломании, ассеты, локализация.

## [1.0.46]
### Интеграция Thaumic Tinkerer — Магнит + закрытие долгов модуля
- **Магнит** (`BlockMagnet` + `TileMagnet`) — работает только при подаче
  редстоуна, радиус действия `сигнал / 2` (как в оригинале), скорость 0.25.
  ПКМ переключает режим притягивание ⇄ отталкивание (в оригинале это бит 0
  метаданных и разворот Techne-модели; у нас — состояние `pulling`, видно по
  текстуре верхней грани: сходящееся ядро против расходящегося кольца).
  Притягивание не трогает предметы ближе одного блока, чтобы куча под магнитом
  не дёргалась.
- **Русская локализация всего TT-модуля.** Контент 1.0.42–1.0.45 (7 фокусов,
  4 утилиты, тёмный кварц, воронка) не имел ни одного ключа в `ru_ru.lang`,
  хотя весь остальной мод переведён. Добавлены все 18 ключей; файлы снова
  синхронны (1589 ключей в обоих).
- **Тёмный кварц и воронка стали крафтящимися.** С 1.0.44/1.0.45 они были
  доступны только в креативе — рецептов не существовало вовсе. Добавлены
  аркано-рецепты (кварц: 4 из ванильного кварцевого блока, резной и колонна из
  него же; воронка — за исследованием `GRATE`), магниту — свой.
- Guard расширен тремя тестами: регистрация и механика магнита, крафтабельность
  всех TT-блоков, наличие локализации в обоих языках.

## [1.0.45]
### Интеграция Thaumic Tinkerer — Воронка (функциональный тайл-блок)
- **Воронка** (`BlockFunnel` + `TileFunnel`, пакеты `blocks/tinkerer` и
  `tiles/tinkerer`) — блок-пылесос: тайл каждые 8 тиков собирает дропнутые
  предметы в области над собой и вставляет их в инвентарь снизу через
  `IItemHandler`-capability (`ItemHandlerHelper.insertItem`).
- Полный тайл-пайплайн: `BlockContainer` + `createNewTileEntity` +
  `getRenderType(MODEL)`, регистрация блока/ItemBlock/тайла
  (`TILE_REGISTRATIONS`), blockstate + cube-модель (top/side) + base
  item-модель + текстуры (из ассетов TT) + lang. Доступен в креативе.
- Первый **функциональный тайл-блок** TT-модуля (валидирован тайл-реестр
  и тик-логика). Guard расширен. Набор зелёный.

## [1.0.44]
### Интеграция Thaumic Tinkerer — тёмный кварц (декор-блок)
- **Тёмный кварц** (`BlockDarkQuartz`, пакет `common/blocks/tinkerer/`) —
  декоративный блок из 3 вариантов (обычный/резной/колонна) через свойство
  `variant` 0–2. Metadata-ItemBlock (`BlockDarkQuartzItem`), регистрация в
  `ConfigBlocks` (поле, init, getAllBlocks, ItemBlock, item-модели меты в
  ClientProxy), blockstate + 3 модели блока + базовая item-модель, текстуры
  из ассетов TT, lang для 3 вариантов. Доступен в креатив-вкладке.
- Guard `ThaumicTinkererFociStaticGuardTest` расширен на блок. Первый блок
  TT-модуля — проверен пайплайн регистрации блоков. Набор зелёный.
- Магнит/ремонтник и др. функциональные блоки (тайлы/кастом-рендер) —
  следующим заходом.

## [1.0.43]
### Интеграция Thaumic Tinkerer — утилитарные предметы (4)
Продолжение интеграции TT (pixlepix/nekosune) нативным модулем. Новый пакет
`common/items/tinkerer/`; текстуры из ассетов TT.
- **Зеркало установки**: sneak-ПКМ по блоку — привязать тип; ПКМ по грани —
  поставить привязанный блок на расстоянии, расходуя совпадающий предмет из
  инвентаря. Тип хранится в NBT.
- **Талисман очищения**: в инвентаре периодически снимает один вредный
  потион-эффект с владельца.
- **Талисман опыта**: ПКМ — сложить весь свой опыт в талисман; sneak-ПКМ —
  забрать обратно. Хранит XP в NBT, показывает полосу заряда + тултип.
- **Кошачий амулет**: в инвентаре обнуляет урон от падения.
- Регистрация в `ConfigItems`, модели+текстуры+lang, аркано-рецепты в
  `ConfigTinkerer` (гейт за существующие исследования MIRROR/BATHSALTS/
  ENCHANT/BOOTSTRAVELLER), вне TC4-парити корпуса.
- Guard `ThaumicTinkererFociStaticGuardTest` расширен на утилиты. Набор зелёный.

## [1.0.42]
### Интеграция Thaumic Tinkerer — фокусы палочки (7)
Реимплементация фокусов из **Thaumic Tinkerer** (авторы pixlepix, nekosune)
нативно под 1.12.2 как модуль внутри мода, поверх нашего API `IWandFocus`.
Оригинальный jar 1.7.10 несовместим с 1.12.2 — фичи переписаны заново,
текстуры взяты из ассетов TT (с атрибуцией).
- **Фокус Переплавки** — правый клик по блоку плавит его (на месте, если
  результат блок: булыжник→камень, песок→стекло; иначе дроп: руда→слиток).
- **Фокус Телекинеза** — притягивает дропнутые предметы и опыт к игроку.
- **Фокус Полёта** — рывок в направлении взгляда, обнуляет урон от падения.
- **Фокус Лечения** — лечит цель под прицелом или самого игрока.
- **Фокус Отражения** — ударная волна, отбрасывает мобов вокруг.
- **Фокус Дислокации** — блинк на блок под прицелом (телепорт).
- **Фокус Эндер-сундука** — открывает эндер-сундук откуда угодно.
- Каждый: класс `Focus*` на `ItemFocusBasic`, регистрация в `ConfigItems`,
  модель+текстура+lang, аркано-рецепт (гейт за тематически близкое
  существующее исследование фокуса). Рецепты в отдельном `ConfigTinkerer`
  вне TC4-парити корпуса — аудиты рецептов остаются на эталоне.
- Guard `ThaumicTinkererFociStaticGuardTest` пинит наличие 7 фокусов,
  регистрацию, рецепты, текстуры/модели/lang. Полный набор зелёный.

## [1.0.41]
### Публичный API для аддонов
- **Документация `API.md`** (корень репо): руководство для аддон-разработчиков —
  как подключить `Thaumcraft-<version>-api.jar`, регистрация аспектов/тегов
  объектов, рецепты (аркана/тигель/инфузия/зачарование), aura/essentia
  (`AuraHelper`/`AspectSourceHelper`), исследования, варп, лут, семена, и полный
  список интерфейсов для реализации (`IWandFocus`, `INode`, `IAspectContainer`,
  `IEssentiaTransport`, `IGoggles`, `IVisDiscountGear`, `IWarpingGear` и др.) +
  события (`AspectRegistryEvent`, `IScanEventHandler`).
- **`apiJar`**: добавлен манифест (`Specification-Title: Thaumcraft|API`,
  версия) и исходники `thaumcraft/api/**` в jar — IDE аддонов получают
  сигнатуры/джавадок.
- **`ThaumcraftApiSurfaceStaticGuardTest`**: пинит публичные сигнатуры
  `ThaumcraftApi`/`AuraHelper`/`AspectSourceHelper`/`AspectEventProxy` +
  Forge `@API`-токен, чтобы сборки аддонов не ломались молча между версиями.
- Проверено: API-поверхность полна (`ThaumcraftApi` — 30+ методов, реальный
  `InternalMethodHandler` проводится при инициализации), api-jar не тянет
  внутренние `common`/`client` классы.

## [1.0.40]
### Адаптировано из FOREVA — connected-текстуры варёного стекла (baked-model инфра)
- **`BlockCosmeticOpaque`** (варёное стекло): extended block state с
  `IUnlistedProperty` соседей — грани соединяются с примыкающими блоками
  варёного стекла (как в оригинале TC4), а не рисуют полную сетку швов.
- **`WardedGlassBakedModel`** (новый): baked-модель, выбирающая один из 47
  спрайтов `warded_glass_N` по маске соседей через `ConnectedTextureUtils`.
- **`ClientModelRegistry`**: регистрация 47 спрайтов `warded_glass_*`,
  константа `WARDED_GLASS_MODEL` и подмена модели в `ModelBakeEvent`.
- Модель `blockcosmeticopaque_2.json` → базовый спрайт `warded_glass_1`;
  guard `wardedGlassTextureShips` обновлён.
- Полный набор тестов остаётся зелёным (0 провалов).
- **Отложено:** eldritch-crust baked-model — конфликтует с нашим фиксом
  `getLightValue` (светокамень в генерации) и меняет default-state/render-type
  BlockEldritch; требует осторожного мёржа. Детали в PORTING_HANDOFF.md.

## [1.0.39]
### Адаптировано из FOREVA — генерация Внешних Земель
- **`GenCommon`**: доработана генерация данжа — ячейки корки (CRUST, feature 7:
  камень→`blockCosmeticSolid` meta 14 + шанс `blockEldritch` meta 4), внешняя
  оболочка из BEDROCK вместо STONE, упрощённая и корректная логика размещения
  спавнеров крабов/декора.
- **`ChunkProviderOuter`**, **`GenBossRoom`**, **`GenPassage`**, **`MazeThread`**:
  версии FOREVA (провайдер чанков измерения, комната босса, коридоры-паутины,
  поток генерации лабиринта).
- **`ConfigEntities`**: константа `MIND_SPIDER_ID` (`legacyPath("MindSpider")`),
  спавнеры web-nest теперь ссылаются на неё; спавн-биомы через
  `BiomeProvider.allowedBiomes`. Guard web-nest обновлён под константу.
- Полный набор тестов остаётся зелёным (0 провалов).

## [1.0.38]
### Адаптировано из FOREVA — пакет блоков
- **`BlockCustomOre`**: hit-эффект `infusedStoneSparkle` при ударе по
  инфьюженному камню (искры цвета по мете). Добавлен прокси-метод
  `infusedStoneSparkle` (Common no-op / Client `FXSparkle`).
- **`BlockCosmeticSolid`**: `addDestroyEffects` — взрыв-пуфф + звук
  `CRAFTFAIL` при разрушении меты 8.
- **`BlockWoodenDevice`**: тихое событие сенсора (`id == -1 || 255`) —
  обновление redstone-сигнала сенсора без ноты; guard-пара синхронизирована.
- **`BlockStoneDevice`**, **`BlockMagicalLog`**, **`BlockAlchemyFurnace`**,
  **`BlockArcaneFurnace`**, **`BlockWarded`**, **`BlockEssentiaReservoir`**:
  приняты версии FOREVA.
- Полный набор тестов остаётся зелёным (0 провалов).
- **Не взято:** FX-частицы/лучи FOREVA — конфликт с нашей локальной
  архитектурой `ITCParticle` (движок владеет отрисовкой). Детали в
  PORTING_HANDOFF.md.

## [1.0.37]
### Адаптировано из FOREVA — пакет тайлов
- **`TileInfusionMatrix`**: принята версия FOREVA (ядро инфузионного алтаря —
  стабильность крафта, обращение с эссенцией и нестабильностью).
- **`TileFocalManipulator`**, **`TileWandPedestal`**: версии FOREVA
  (манипулятор фокусов, пьедестал — зарядка/логика).
- **`TileEldritchCrabSpawner`**: версия FOREVA (спавнер крабов данжа).
- **`TileSensor`**, **`TileOwned`**: версии FOREVA; guard-пара сенсора
  синхронизирована.
- Полный набор тестов остаётся зелёным (0 провалов).

## [1.0.36]
### Адаптировано из FOREVA — пакет рендереров
- **Узлы** (`TileNodeRenderer`, `TileNodeEnergizedRenderer`,
  `TileNodeStabilizerRenderer`): новый API `renderNodeSeeded` — фаза анимации
  сидится мировыми координатами (стабильна при движении камеры);
  GL-гигиена: save/restore cull/lighting, fullbright-пузырь стабилизатора
  с восстановлением лайтмапы. Guard обновлён под `renderNodeSeeded`.
- **Тигель** (`TileCrucibleRenderer`): per-vertex освещение поверхности
  жидкости (`DefaultVertexFormats.BLOCK` + `.lightmap()` вместо глобального
  `setLightmapTextureCoords`) и гейт `hasWater()` — поверхность рисуется
  только при реальной воде, как в TC4. В `TileCrucible` добавлен `hasWater()`.
- **Клапан трубы** (`TileTubeValveRenderer`): экструзия спрайта через общий
  `ExtrudedSpriteRenderHelper` (инфра 1.0.27) вместо ручной геометрии.
- **Кристаллы** (`ItemCrystalRenderer`): версия FOREVA.
- **Warded-блоки** (`TileWardedRenderer`): connected-textures через новую
  инфру `ConnectedTextureUtils` (соединённые текстуры печатей как в TC4).
- Guard-пары рендерер+тест синхронизированы с FOREVA (crucible, tube, warded).
  Полный набор остаётся зелёным (0 провалов).

## [1.0.35]
### Исправлено — eldritch-оболочки + тестовая база 0 провалов
- **Eldritch-блоки meta 4/5/6** (`blockeldritch_4/5/6.json`): возвращена
  элементная геометрия вместо плоского `cube_all` — оболочка утоплена по
  бокам (`2..14`), но прижата к полу (`y: 0..14`, а не парит на 2px над
  опорой, как в свежей FOREVA). Восстанавливает наш фикс из задачи
  «парящие eldritch-блоки», потерянный при откате массового копирования.
- **Эталонное дерево `mod/thaumcraft_src/`** (1096 файлов, ~15 МБ,
  решение FOREVA): декомпилированные ассеты оригинального TC4 для
  fidelity-тестов (obj-модели, текстуры). Озеленяет последние 6 тестов:
  BlockTextureAssetCoverage, FluxReservoirRendererFidelity,
  FocusExcavation/FocusFrost VisualParity, RotaryMachineShell,
  AlchemyFurnaceAdvancedRendererContract.
- **Итог: полный тестовый набор зелёный (0 провалов)** — впервые.
  Путь: 21 → 17 (scripts/docs) → 14 (CRLF lang + таумометр) → 13 (UTF-8
  компиляция) → 7 (LF-нормализация исходников) → 6 (eldritch) → 0
  (thaumcraft_src).

## [1.0.34]
### Адаптировано из FOREVA — Таумономикон + фикс залипшего каста палочки
- **`ClientTickEventsFML`**: release-latch палочки — ванила шлёт
  RELEASE_USE_ITEM только пока клиент считает руку активной; синхронизация
  NBT/руки могла сбросить флаг до отпускания кнопки, и серверный канал
  каста жил вечно. Теперь удержание отслеживается защёлкой, и обычный
  релиз отправляется один раз на key-up (пара к фиксу фокусов 1.0.30).
- **`GuiResearchRecipe`**: принята версия FOREVA — убран фоновый
  `MappingThread` (hash→ItemStack кэш); рендер страниц рецептов идёт без
  предварительного маппинга. `client/gui/MappingThread.java` удалён,
  бутстрап потока из тик-хендлера убран.
- **`build.gradle`: `options.encoding = 'UTF-8'`** для всех JavaCompile —
  на Windows javac брал системную кодировку (cp1251) и портил `§`-литералы
  в guard-тестах (ложный провал `GuiResearchRecipeStaticGuardTest` при
  корректном контенте). FOREVA не замечает это, живя на Linux.
- **Тестовая база: 14 → 13** (GuiResearchRecipeStaticGuardTest зелёный).

## [1.0.33]
### Адаптировано из FOREVA — таумометр (скан + рендер)
- **`ItemThaumometer`**: принята версия FOREVA — валидация аспектов один раз
  за попытку скана (`doActiveScan(..., notifyInvalid)`), скан узлов доходит до
  сервера даже если аспекты узла ещё не синхронизированы клиенту
  (`isNodeScan`-путь), защищённый pick-block скан блоков с сортировкой
  кандидатов по аспектам.
- **`ItemThaumometerRenderer`**: рендерер сам вычисляет цель скана (`doScan`)
  вместо чтения состояния предмета (`getActiveScan` убран); TC6-донорские позы
  TEISR для GUI/земли/фиксации/третьего лица (`itemthaumometer_tesr.json`:
  поворот 90° и трансляция экрана). Поверх FOREVA возвращён save/restore
  лайтмапы (`prevLightX/Y` + `setLightmapTextureCoords` при выходе) — у FOREVA
  была утечка GL-состояния, которую ловит наш guard-тест
  (`HungryNodeAndGlStateParityStaticGuardTest`).
- **Тестовая база: 17 → 14.** Плюс `.gitattributes` (`*.lang text eol=lf`) —
  CRLF-чекаут ломал `\n`-якорные проверки (`CreativeTabVisualParity` прошёл
  без изменения контента). Зелёными стали: CreativeTabVisualParity,
  ItemThaumometerStaticGuard, ThaumometerItemRendererContract.

## [1.0.32]
### Адаптировано из FOREVA — таинт (падение/каскад) + flux scrubber
- **`BlockTaint`: падение и каскад корки** (классика TC4). Таинтовая корка
  (meta 0) теперь падает как песок через `EntityFallingTaint`, а если под ней
  нет опоры, но сбоку есть воздух над колонной таинта — каскадит вбок
  (спред вниз по склону). Уточнён `canFallBelow`: сквозь воздух, огонь,
  таинт-фибры, заменяемые блоки, воду и лаву; флюкс-гу уровня ≥4 — стоп.
  Убрана дублирующая локальная `isTaintBiome` (везде `BlockTaintFibres`).
- **`EntityFallingTaint`**: физика приземления (демпфинг motion), корректная
  обработка поршней, `world.mayPlace` вместо устаревшего `canPlaceBlockAt`.
- **`BlockFluidDeath`**: `randomDisplayTick` — пузыри (`slimyBubble`) и редкий
  звук лавы для жидкости смерти. Добавлен прокси-метод `slimyBubble`
  (Common no-op / Client `FXSlimyBubble`).
- **`TileFluxScrubber`**: FX-эффект очистки вынесен в `sendFluxCleanupEffect`
  и теперь бьёт по позиции очищаемого флюкса (`target`), а не по координатам
  скраббера. Guard-тест обновлён.

## [1.0.31]
### Адаптировано из FOREVA — система варпа
- **Сообщения варпа → HUD-оверлей** (`PacketWarpMessage`): вместо спама в чат
  (`TextComponentTranslation` + `sendMessage`) уведомления идут через
  `PlayerNotifications.addNotification` (всплывающий оверлей), сохраняя
  прокси-границу `Thaumcraft.proxy.scheduleClientTask`. Разгружает чат
  (частично закрывает задачу «наложение чата»). Логика уточнена: временный
  варп уведомляет только при получении, липкий — в обе стороны, обычный —
  при получении; шёпот (`TCSounds.WHISPERS`) вынесен в `@SideOnly` хелпер.
- **Неизлечимые варп-эффекты** (`WarpEvents`): дебаффы варпа (VisExhaust,
  Thaumarhia, SunScorned, InfectiousVisExhaust, DeathGaze) накладываются через
  `applyPotionWithoutCuratives` — очищается список curative items, молоко и
  прочие «лекарства» их не снимают (как в оригинале). UnnaturalHunger получает
  ambient-флаг.
- **Death Gaze учитывает видимость**: таргетинг `checkDeathGaze` теперь
  проверяет `canBeCollidedWith` и конус обзора игрока
  (`EntityUtils.isVisibleTo(0.75F, …)`) и помечает игрока последним атакующим
  (`setLastAttackedEntity`). Добавлен `EntityUtils.isVisibleTo` (FOV-проверка).

## [1.0.30]
### Адаптировано из FOREVA — фокусы палочки (раскопки/огонь/шок)
- **Поддержка оффхенда**: `FocusExcavation`/`FocusFire`/`FocusShock` теперь
  берут руку через `ItemWandCasting.getHandHoldingWand` вместо жёсткого
  `MAIN_HAND` — фокусы корректно работают с палочкой в левой руке
  (свинг, активная рука).
- **Серверное владение ресурсами**: расход виса и остановка использования
  (`consumeAllVis`/`stopActiveHand`) обёрнуты в `!world.isRemote`. Раньше
  клиент чистил использование локально → ванила слала RELEASE_USE_ITEM, а
  серверный канал непрерывного каста «висел» бесконечно (баг в мультиплеере).
- **`resetActiveHand()` → `stopActiveHand()`**: корректно триггерит остановку
  использования предмета (нужный колбэк), сохраняя релиз-пакет и позу
  активного использования в рендере.
- **FocusExcavation**: `setBlockToAir` переставлен после расчёта дропа блока.

## [1.0.29]
### Адаптировано из FOREVA — узлы-в-банках
- **`BlockJar`**: добавлен полноценный `harvestBlock` (обработка Fortune +
  `fireBlockHarvesting`, банка с содержимым корректно дропается при ломании —
  раньше был только `getDrops`, который в 1.12.2 может обходиться харвест-путём
  и ронять пустую банку) и `getPickBlock` (средний клик берёт заполненную
  банку с аспектами/узлом, а не пустую). Дроп-логика вынесена в хелперы
  `createJarDrop`/`storeFillableData`/`storeNodeData`. Партиклы разрушения
  подавляются для meta 15.
- **`TileJarNode`**: реализует `IWandable` — палочкой по банке узел
  **выпускается обратно в мир** (создаётся `blockAiry` с сохранёнными
  аспектами/типом/модификатором/id, звук и партиклы бьющегося стекла).
- **`BlockJarItem`**: тултипы банки — тип и модификатор узла, список аспектов
  (с гейтом по знанию: неизвестные показываются как «unknown»), фильтр-аспект.
- **`ItemWandCasting.getHandHoldingWand`**: добавлен статический хелпер
  определения руки с палочкой (нужен `TileJarNode` и будущим фокусам FOREVA).

## [1.0.28]
### Адаптировано из FOREVA — chop-парити големов + топор
- **Harvest-AI големов** (`AIHarvestLogs`, `AIHarvestCrops`): взяты версии
  FOREVA — теперь големы-лесорубы валят дерево целиком (chop-парити), а
  големы-жнецы корректно собирают урожай. Ранее было отложено (1.0.27) из-за
  конфликта сигнатур `BlockUtils`.
- **`BlockUtils` + Топор Стихий**: приняты версии FOREVA единым согласованным
  набором, чтобы сигнатуры совпали (`breakFurthestBlock(world, pos, player)`,
  `harvestBlock(world, pos, player)`, `destroyBlockPartially`). Топор FOREVA
  проверен пользователем в игре — валка/магнит/пузырьки работают. Это боковая
  замена нашей реконструкции 1.0.15 на оригинальную реализацию FOREVA.
- Guard-тест топора (`ItemElementalAxeStaticGuardTest`) обновлён под новый
  3-арг вызов `breakFurthestBlock`; остальные контракты (магнит, ремонт
  тауминием, FX-пакет валки) сохранены.

## [1.0.27]
### Адаптировано из FOREVA — зеркала и големы
- **Зеркала** (магическое/эссенции): перенесены `BlockMirror`, `TileMirror`,
  `TileMirrorRenderer`, `BlockMirrorItem` + инфра `ExtrudedSpriteRenderHelper`
  (рендер рамок/панелей через экструзию спрайтов). Забирает фиксы FOREVA:
  рамки зеркал, панели/частицы, связанный портал, стабильность рендера и
  вывода предметов, детали связанных зеркал.
- **Големы**: перенесены `RenderGolemBase` и `AIGolemAttackOnCollide` — фиксы
  FOREVA по guard-атакам (мили-атака сохраняется, когда путь оставляет цель в
  досягаемости) и рендеру голема.
- Harvest-AI големов (chop-парити) не взят — версия FOREVA завязана на её
  `BlockUtils`, разошедшийся с нашим (наш фикс валки топором); отложено во
  избежание конфликта.

## [1.0.26]
### Исправлено (из фиксов апстрима FOREVA)
- **Во ВСЕХ GUI-контейнерах Thaumcraft не показывались тултипы предметов при
  наведении** (магический верстак, тауматорий, печь, бур, зеркало, големы, спа,
  манипулятор фокусов, деконструктор и др.). Классы `GuiContainer` не
  переопределяли `drawScreen` с вызовом `renderHoveredToolTip` — стандартная
  ваниль рисует тултип слота именно там. FOREVA чинил это для верстака и
  ховер-ранца; здесь добавлено всем 14 контейнерам. Теперь при наведении на
  предмет (в т.ч. в верстаке) видно его подсказку, включая стоимость/скидку Вис.

## [1.0.25]
### Исправлено (из фиксов апстрима FOREVA)
- **Резервуар эссенции «не принимал» эссенцию — на деле не синхронизировал
  содержимое клиенту.** Наш порт при изменении содержимого звал только
  `markDirty()` (сервер), а FOREVA — `markDirtyAndSync()` (+ `notifyBlockUpdate`).
  Без синхронизации клиент никогда не видел накопленную эссенцию → жидкость не
  рисовалась → выглядело как «резервуар ничего не принимает». Добавлен helper
  и применён в `addToContainer`/`takeFromContainer` (фикс FOREVA «sync reservoir
  contents»). Теперь налитая эссенция видна.

## [1.0.24]
### Исправлено (адаптированы решения из апстрима FOREVA)
- **Корень проблем с OBJ-моделями найден: CCL разворачивает намотку граней при
  парсинге OBJ.** Отсюда «наизнанку» и «чёрный» рендер (нормали смотрят внутрь →
  GL-освещение красит модель в чёрный). Наш порт отошёл от FOREVA и потерял
  восстановление намотки.
  - **Улучшенная алхимическая печь**: рендерер заменён на версию FOREVA с
    `restoreObjFaceOrder` (backfacedCopy + инверсия нормалей при загрузке) —
    чистое решение вместо накопленных хаков (disableCull/forced lightmap).
    Culling штатный, освещение штатное, полный save/restore состояния.
  - **Резервуар эссенции**: шелл рисуется с `disableCull` (как FOREVA) + яркость
    блока по `getCombinedLight` — «ледяной»/вывернутый вид устранён.
- **Тауматорий: чёрная модель.** Рендерер возвращён к версии FOREVA (plain
  `renderAll`); мой прошлый фикс с `getCombinedLight` затемнял модель ночью.
  Первопричина черноты — утечка GL-состояния от хакнутого рендерера печи
  (не восстанавливал lighting/lightmap/cull); адаптация рендерера печи FOREVA
  с полным save/restore это устраняет.

### Проверено против FOREVA
- Свечение узлов ауры (`TileNodeRenderer`) идентично FOREVA/оригиналу — не
  регрессия (у FOREVA файл крупнее лишь из-за новой фичи «узлы в банках сквозь
  блоки»). Блок-свет узла (8) совпадает с оригиналом.
- Плитка авто-печи (`TileAlchemyFurnaceAdvanced`) байт-в-байт совпадает с
  FOREVA — условия переплавки и дрейн FIRE/ENTROPY/WATER идентичны; работа
  зависит от аспектов возбуждённого узла.

## [1.0.23]
### Исправлено
- **Резервуар эссенции рендерился «ледяным» (пересвеченным).** OBJ-шелл не
  выставлял lightmap и наследовал полную яркость от предыдущего TESR — светлый
  металл резервуара выбеливался. Выставляется реальная яркость блока
  (`getCombinedLight`) с восстановлением, как у тауматория/печи.

### Проверено — логика приёма эссенции корректна
- `TileEssentiaReservoir` (`fillReservoir`/`addToContainer`/suction) структурно
  совпадает с оригиналом: резервуар каждые 5 тиков тянет 1 эссенцию с грани
  `facing`, если сосед может отдать и его suction ниже. По умолчанию `facing`
  = DOWN (в блок снизу) — резервуар надо повернуть жезлом к источнику. «Не
  наливается из авто-печи» = печь не производит эссенцию (нужны FIRE+ENTROPY+
  WATER из вис-сети в достаточном количестве; см. открытый пункт по печи).

## [1.0.22]
### Исправлено
- **Не показывался тултип «Уменьшение затрат Вис» у гира.** Броня/предметы,
  реализующие `IVisDiscountGear`, должны добавлять строку `tc.visdiscount: X%`
  в подсказку (как в оригинале), но у части предметов `addInformation` был
  вовсе не переопределён. Добавлено для наголенников/робы культиста, очков
  гоблина-гогглов и обычной робы (у пустотной робы и ховер-ранца уже было).
  Теперь при наведении (в т.ч. в магическом верстаке) видно скидку Вис.
  Стоимость Вис жезла и фокусов уже отображалась (см. 1.0.15).

## [1.0.21]
### Исправлено
- **Вис-каналы: размещались всегда «вверх», а не по клик-грани.** Оригинал ставит
  `orientation = сторона клика` (канал крепится к поверхности, как рычаг —
  штатное поведение TC4), но порт этого не делал → канал всегда торчал вверх.
  Сторона клика теперь передаётся из `getStateForPlacement` в `onBlockPlacedBy`
  и применяется к `TileVisRelay`.
- **Вис-каналы: не рисовались линии сети между каналами/узлами.** В `TileVisRelay`
  был потерян `drawEffect()` (клиентский рендер луча к родителю) и синхронизация
  смещения родителя через NBT (`px/py/pz`) — клиент не знал родителя и не рисовал
  луч. Восстановлены: `drawEffect` через `proxy.beamPower`, NBT-синхронизация
  родителя, пульсация цвета аспекта (`addPulse`/`receiveClientEvent`), relight
  в `parentChanged`.
- **Возбуждённые (энергизированные) узлы: добавлен render bounding box.**
  `TileNodeEnergized` не переопределял `getRenderBoundingBox`, из-за чего TESR
  узла (свечение + кольцо молний) мог отсекаться. Бокс растянут на 8 блоков,
  как у обычного узла.
- **Вис-сеть не формировалась → авто-алхимическая печь (и др. потребители Вис)
  не работали.** `ThaumcraftApiHelper.rayTraceIgnoringSource` вопреки названию
  НЕ игнорировал исходный блок (порт заменил кастомный raytrace оригинала на
  ванильный `world.rayTraceBlocks`). Луч проверки видимости `canNodeBeSeen` от
  вис-канала (у которого есть коллизия-бокс) к узлу «попадал» в сам канал →
  видимость ложно-ложна → канал не подключался к узлу → сеть не строилась, и
  печь не получала тепло/энергию (FIRE/ENTROPY/WATER). Восстановлен пропуск
  исходного блока (ретрейс за пределы его ячейки), как в оригинале.

## [1.0.20]
### Исправлено
- **Улучшенная алхимическая печь выглядела «вывернутой наизнанку».**
  `adv_alch_furnace.obj` намотан в обратную сторону (CW) по сравнению с другими
  OBJ порта (резервуар/сканер рендерятся тем же путём CCModel корректно), из-за
  чего back-face culling отсекал ВНЕШНИЕ грани и показывал внутренность модели.
  Для этой модели culling отключён (рисуются обе стороны); состояние cull
  сохраняется и восстанавливается.

## [1.0.19]
### Исправлено
- **Улучшенная алхимическая печь стала прозрачной** (после фикса яркости 1.0.18).
  OBJ-модель корпуса/баков непрозрачна, но наследовала blend-состояние из
  world-render прохода (или от квадов бака/огня) — печь просвечивала. Перед
  отрисовкой модели теперь явно выставляется непрозрачный проход
  (`disableBlend`, `enableDepth`, `depthMask(true)`).
- **Тауматорий (авто-алхимия): чёрная модель.** Это ОТДЕЛЬНЫЙ блок
  (`metal_device` meta 10/11), не путать с алхимической печью.
  `ModelThaumatorium` (ModelBase) не несёт lightmap; в 1.7.10 TESR освещал
  модель по умолчанию, в 1.12 координаты lightmap оставались на ambient — вся
  башня чёрная. Выставляется яркость блока (`getCombinedLight`) + отключается
  GL-освещение на время отрисовки, как в других модельных рендерерах.

## [1.0.18]
### Исправлено
- **Улучшенная (авто) алхимическая печь всё ещё рендерилась чёрной.** Фикс
  1.0.16 (отключение GL-освещения) был недостаточен: OBJ-модель рисуется
  форматом `OLDMODEL_POSITION_TEX_NORMAL` без per-vertex цвета и lightmap, но
  всё равно домножается на lightmap-текстуру мира, координаты которой остались
  на почти нулевой (ambient) яркости — отсюда чёрная модель. Квады бака/огня
  работали, потому что сами выставляли яркость перед отрисовкой. Теперь перед
  отрисовкой модели lightmap принудительно ставится в full-bright
  (восстанавливается в finally).

## [1.0.17]
### Исправлено
- **Таумометр: пропал звук сканирования.** На клиенте вызывался
  `world.playSound(player, …)` с локальным игроком первым аргументом — эта
  перегрузка ИСКЛЮЧАЕТ переданного игрока (он якобы уже слышал звук локально),
  поэтому на клиенте тик-звук `cameraticks` не проигрывался вообще. Передаётся
  `null` (оригинал использовал клиентский `func_72980_b`).
- **Оверлей фокуса укрепления (архитектор) восстановлен.** Вся логика
  (`getArchitectBlocks`/`showAxis`) в порту была, но рендер отсутствовал —
  добавлен обработчик `DrawBlockHighlightEvent` для IArchitect-жезлов: каждый
  затрагиваемый блок подсвечивается пульсирующим боксом с текстурой
  укреплённого стекла (цвета пульса как в оригинале). Оригинал рисовал через
  connected-textures RenderBlocks (в 1.12 этого пайплайна нет) — сделан
  эквивалентный по виду per-block оверлей.
- **Столбы из брёвен на кронах Волшебного леса.** `checkBlockLine` в
  `WorldGenBigMagicTree` считал проходимым дерево (`isReplaceable` включает
  wood), а оригинал — только воздух/листву. Из-за этого ветви пробивали
  соседние стволы и оставляли перепутанные брёвна-шипы на кроне. Возвращена
  проверка воздух-или-листва.

### В работе (следующий набор)
- Остальная потерянная система оверлеев: стрелки осей архитектора
  (`architect_arrows.png`) и маркеры голема (`mark.png`/`home.png` над
  отмеченными блоками) — отдельная инфраструктура ItemGolemBell, требует
  проверки в игре.

## [1.0.16]
### Исправлено
- **Далёкие земли: светящийся заросший камень (и другой светящийся декор)
  рендерился чёрным.** Классическая ловушка 1.12: при генерации чанка начальный
  blocklight берётся из stateless-перегрузки `getLightValue(IBlockState)`,
  а порт переопределял только версию с world/pos — сгенерированные светящиеся
  блоки оставались неосвещёнными (чёрный куб в тёмном коридоре). Добавлена
  stateless-перегрузка в `BlockEldritch` и `BlockCrystal` (кластеры в пещерах
  тоже светятся с генерации). Действует для новых чанков; в старых — после
  любого обновления блока рядом.
- **Далёкие земли: кластеры кристаллов отваливались предметом.** Порт
  drop-чекал ВСЕ меты при любом изменении соседей и при onBlockAdded; в
  оригинале проверяются только меты ≤ 6 (сбалансированные кластеры гнёзд,
  мета 7, не проверяются вовсе), только по стороне крепления из тайла и без
  проверки на добавление — иначе кластеры срывались во время генерации
  соседних ячеек лабиринта. Восстановлено поведение оригинала.
- **Улучшенная алхимическая авто-печь рендерилась чёрным.** В 1.7.10 OBJ-модель
  рисовалась фактически без GL-освещения (яркость из lightmap); порт рендерил
  с включённым lighting и нормалями CCModel — вся модель уходила в чёрный.
  Освещение отключается на время отрисовки модели (восстанавливается в finally).
- **Таумометр: плавный перевод скана на новую цель.** При переводе взгляда на
  другой блок во время удержания ПКМ скан теперь перезаряжается на новую цель
  без сброса руки/анимации (заряд отслеживается от момента захвата цели,
  20 тиков наведения; длительность использования сделана неограниченной).

## [1.0.14]
### Исправлено
- **Голодный узел ничего не делал.** При портировании потерялись оба
  обработчика из TC4: `handleHungryNodeFirst` (каждый тик: частицы-крошки
  блоков, летящие к узлу; при `hardNode` — втягивание сущностей в радиусе 15
  и поедание тех, кто ближе 2 блоков, с поглощением случайного примального
  аспекта жертвы) и `handleHungryNodeSecond` (раз в 50 тиков разрушает
  случайный блок в радиусе 16 с твёрдостью [0..5)). Оба восстановлены по
  оригиналу, добавлен `hungryNodeFX` в прокси (FXBoreParticles к центру узла).
- **Чёрные слоты в инвентаре от предметов мода.** `ItemThaumometerRenderer`
  трижды переопределял lightmap под «свечение сканера» и не восстанавливал —
  после отрисовки таумометра в слоте все последующие слоты рисовались с чужой
  яркостью. Lightmap теперь сохраняется/восстанавливается (try/finally).
  Та же утечка закрыта в `TileEssentiaCrystalizerRenderer` (мир, не GUI).
- Guard-тест `HungryNodeAndGlStateParityStaticGuardTest` на оба контракта.

### Проверено, расхождений не найдено
- **Палочки/посохи на земле**: код-дефолты GROUND совпадали с оригиналом,
  реальную проблему (масштаб/утопленность после finalRotate) закрыл
  параллельный фикс калибровки в 1.0.13 (`wand_casting.json`).
- **Мнемоническая матрица**: куб 10×10×10 с текстурой brainbox (byte-identical
  оригиналу) + отросток по facing через TESR — структура как в TC4.
- **Двери-печати боссов (EldritchLock)**: ModelCube и все 4 текстуры
  (eldritch_cube/tunnel/particlefield/particlefield32) идентичны оригиналу.
- **Авто-алхимическая печь**: OBJ+текстуры на месте, дрейн вис (FIRE/ENTROPY/
  WATER по 50 раз в 5 тиков) и лимиты совпадают с оригиналом.
- **Големы**: 15/15 классов, структурный паритет.
  Для всего перечисленного нужны скриншоты/симптомы, если визуально всё ещё
  неправильно (см. KNOWN_ISSUES).

## [1.0.13]
### Исправлено (визуал, сверка с оригиналом 4.2.3.5 по байткоду)
- **Пьедестал: предмет визуально двоился.** В `TilePedestalRenderer` сохранилась
  ветка 1.7.10 для Fast-графики: вторая копия предмета, повёрнутая на 180°
  (в 1.7.10 плоские спрайты были односторонними). В 1.12.2 предметы — всегда
  3D-модели, и вторая копия рисовалась как заметный «двойник» крест-накрест.
  Ветка удалена.
- **Брошенный жезл/скипетр/посох: огромный и утоплен в пол.** Калибровка
  `GROUND` в `wand_casting.json` держала масштаб 1.0 со смещением +1 по Y: после
  переворота модели (finalRotate 180°X) жезл (высота модели ~1.3 блока) торчал
  на ~0.3–0.4 блока НИЖЕ точки дропа, посох (~2.7 блока с растянутым стержнем) —
  ещё сильнее. Теперь GROUND: жезл scale 0.5 / translate +0.7, посох scale 0.35 /
  translate +0.5 — модель целиком над полом, размер сопоставим с ванильными
  дропами. Аналогично поправлен `FIXED` (рамки): жезл 0.5/+0.25, посох
  0.35/−0.1 — по центру рамки. Значения можно донастроить в игре без пересборки:
  `config/thaumcraft/render_calibration/wand_casting.json` + F3+T.
- **Обсидиановый тотем и обсидиановая плитка были перепутаны моделями**
  (`blockcosmeticsolid_0` ↔ `_1`). Из-за этого площадка Странного алтаря
  (генератор кладёт мету 1 — плитку) выглядела сложенной из тотемов, а
  обсидиана «почти не было». В оригинале `getIcon`: мета 0/1/8 → base
  `obsidiantile`, резьба тотема — контекстные иконки `obsidiantotem*`
  (`func_149673_e`). Модели разменяны: мета 0 = тотем (колонна
  totem1/totem2), мета 1 = плитка `obsidiantile` во все стороны.
- **Древний камень и древняя порода (меты 11–13 `blockCosmeticSolid`,
  Далёкие земли): неверные текстуры.** Порт использовал `es_5`/`es_6`
  (текстуры стен эльдрич-лабиринта) для меты 12/13. По оригинальному
  `getIcon`: мета 11 и 13 → `es_1`, мета 12 → `er_1`. Проверка индексации по
  соседям: мета 14 → `crust` и мета 15 → `es_p`+`es_1` совпадают с уже
  верными моделями порта. Вдобавок восстановлена вариативность оригинального
  ISBRH: меты 11/13 — случайный выбор `es_1..es_4`, мета 12 — `er_1..er_4`
  (random-варианты в blockstate).
- **Укреплённое стекло и янтарные блоки рендерились непрозрачными.**
  1.0.12 вернул недостающую текстуру `wardedglass.png`, но
  `BlockCosmeticOpaque` в порту не переопределял слой рендера: в оригинале
  весь блок рисуется в проходе 1 (`getRenderBlockPass()==1`, translucent) с
  `isOpaqueCube=false` и glass-style скрытием граней между одинаковыми
  соседями (`func_149646_a`). Полупрозрачные пиксели текстур (стекло, янтарь)
  на solid-слое запекались как грязный непрозрачный камень. Добавлены
  `getRenderLayer()=TRANSLUCENT`, `isOpaqueCube=false`,
  `shouldSideBeRendered` (скрытие граней между блоками той же меты).

## [1.0.12]
### Исправлено
- **Меч Бури: вместо дыма при полёте рисовался «ветер».** `FXSmokeSpiral` был
  портирован на слой 3 (атлас `particles2.png`) с собственным begin/draw, тогда
  как в оригинале это TC-частица слоя 1 (`particles.png`, обычный блендинг),
  рисующая вершины в общий буфер движка. Класс переведён на `ITCParticle`
  (слой 1) по контракту `ParticleEngine`. Плюс: наземный дым у ног снова
  ванильный `smoke` (как `world.spawnParticle("smoke")` в оригинале), а не
  суррогат из generic-частиц.
- **Кирка Стихий: неправильные текстуры индикаторов руд.** Оверлей скана
  семплировал весь столбец `nodes.png` (v 0..1) вместо строки 0 сетки 32×32,
  как в `UtilsFX.renderFacingStrip(frames=32, strip=0)`. Плюс опечатки в цветах
  подсветки воды/лавы: `0x3CD4BC`→`0x3CD4FC` (3986684), `0xFF55C1`→`0xFF5A01`
  (16734721).
- **Далёкие земли: блок пустоты не имел коллизии.** В оригинале
  `BlockEldritchNothing` — твёрдый блок (коллизия-куб с отступом 0.125 со всех
  сторон, `func_149668_a`), нулевой только selection box; порт ошибочно сделал
  его полностью проходимым. Возвращена коллизия и контактный урон 8.0
  (OUT_OF_WORLD) для не-креативных сущностей старше 20 тиков.
- **Далёкие земли: не спавнились пауки в паутинных гнёздах (feature 14).**
  Спавнер указывал на `thaumcraft:mind_spider`, а сущность зарегистрирована как
  `thaumcraft:mindspider` (`legacyPath` — lowercase без подчёркиваний) —
  спавнер был пуст. Исправлен id.
- **Укреплённое стекло: отсутствовала текстура.** Модель
  `blockcosmeticopaque_2.json` ссылается на `blocks/wardedglass`, но файл
  `wardedglass.png` не был перенесён из оригинальных ассетов (были только
  пофреймовые `warded_glass_N.png`) — скопирован из эталона 4.2.3.5.
- Аудит остальных стихийных инструментов (топор/лопата/мотыга/FXSparkle/FXWisp/
  FXVent и др.): маршруты и слои частиц совпадают с оригиналом, правок не
  потребовалось. Guard-тест `FxLayerAndEldritchParityStaticGuardTest` фиксирует
  все перечисленные контракты.
- **Адская печь: лицо-пасть снова стало плоским.** В `ArcaneFurnaceBakedModel`
  все три слоя сопла были прижаты к внешней поверхности (z=13–15), тогда как в
  оригинальном `BlockArcaneFurnaceRenderer` пасть глубокая: решётка
  (`furnace13`) утоплена на 10/16 (плоскость z=6), морда (`furnace15`) — на
  12.8/16 (z=3.2), ванильный огонь — на 14.4/16 (z=1.6) и рисуется высотой
  1.5 блока (в TC4 для огня выставляются баунды 0..1.5). Порядок слоёв из
  фикса 1.0.2 сохранён, восстановлены оригинальные глубины и высота огня.
- **Инфузная руда: прожилки кристаллов просвечивали в руке/инвентаре.**
  Запечённые текстуры `infusedorestone_<аспект>.png` унаследовали от исходного
  `infusedorestone.png` полупрозрачную альфу в прожилках (298 пикселей, альфа
  до 89): в мире solid-слой альфу игнорирует, а при рендере предмета в руке/GUI
  включён блендинг — прожилки становились полупрозрачными. Альфа выровнена в
  255 с сохранением RGB — вид в мире не изменился, в руке блок теперь плотный.
  Киноварь и янтарная руда проверены — у них альфа была корректной.

- **Боссы Далёких земель показывали имя «%s - %s».** У Стража и Голема Древних
  потерялись переопределения `generateName()` (базовая реализация пустая) —
  имя никогда не форматировалось, и клиент показывал сырой шаблон из lang.
  Восстановлены по оригиналу: Страж — `String.format(<ключ>, титул,
  champion-модификатор)`, Голем — `String.format(<ключ>, champion-модификатор)`.
  У Претора культистов реализация была на месте.
- **Локализация: аудит ключей и форматов.** Все ключи, используемые кодом,
  присутствуют в обоих lang-файлах; `I18n.format` без аргументов на ключах с
  плейсхолдерами не осталось; `%n`-ключи (`tc.addaspectdiscovery`,
  `tc.addaspectpool`, `tc.forbidden`) обрабатываются ручной подстановкой.
  Два захардкоженных английских сообщения переведены на ключи:
  `tc.arcanedoor.locked` («Дверь не поддаётся.») и `tc.wardedpearl`
  (уничтожение жемчуга Края варденым блоком) + русские переводы.

### Известное / не воспроизведено
- «Портал в Далёкие земли открывается раз через раз» — статически расхождений
  с оригиналом не найдено (checkForMaze прогревается при вставке глаз, телепорт
  и MazeThread идентичны). Нужны детали воспроизведения (см. KNOWN_ISSUES).
- «Меню выбора набалдашника не показывает инфо», «проблемы с кастом всех
  набалдашников», «сообщения в чате накладываются» — статический аудит
  радиала, ItemWandCasting (setActiveHand/onUsingTick/стоимости всех 10
  фокусов) и уведомлений расхождений с оригиналом не нашёл. Нужны детали
  (см. KNOWN_ISSUES).

## [1.0.11]
### Исправлено
- **Биомы: трава и листва были назначены наоборот — направление исправлено
  (поверх аудита 1.0.9).** Аудит 1.0.9 верно нашёл побитые hex-константы и
  перепутанные температуру/влажность, но принял `func_150571_c` за траву, а
  `func_150558_b` за листву. По `mappings/methods.csv` наоборот:
  `func_150558_b` = `getBiomeGrassColor`, `func_150571_c` = `getBiomeFoliageColor`.
  Независимое подтверждение — донор TC6 (нативный 1.12.2 код Azanor):
  `getGrassColorAtPos` (`func_180627_b`, сверено по MCP stable_39) возвращает
  те же 5635969 (`0x55FF81`), `getFoliageColorAtPos` — 6750149 (`0x66FFC5`),
  у Eerie трава `0x404840`. Итоговые значения:
  Волшебный лес — трава `0x55FF81`, листва `0x66FFC5` (blueBiome: `0x66AACC`/`0x77CCEE`);
  Eerie — трава `0x404840`, листва `0x405340`;
  Taint — возвращены исходные (правильные) трава `0x6D4189`, листва `0x7C6D87`
  (обмен из 1.0.9 откатен; порт здесь изначально был верен).
  Guard-тест `biomeGrassFoliageWaterColorsMatchTC4` теперь фиксирует привязку
  цвета к конкретному методу для всех трёх биомов.

### Добавлено
- **Аудит аспектов новоделов 1.8–1.12.2 (повторный проход): закрыта губка.**
  Сверен полный список ванильных блоков/предметов 1.8–1.12.2 с регистрациями в
  `ConfigAspects.registerModernVanilla()` с учётом автогенерации аспектов из
  рецептов (крафт/тигель/арканный/инфузия; печные рецепты НЕ обходятся).
  Единственный найденный пробел — губка (обе меты): в 1.7.10 была недоступна,
  поэтому в TC4 тега нет, с 1.8 мокрая падает с древних стражей, сухая — только
  через печь → аспекты ниоткуда не выводились, таумометр её не сканировал.
  Добавлены теги из донора TC6 (BETA26): сухая EARTH 5 + TRAP 5 + VOID 5,
  мокрая EARTH 5 + TRAP 5 + WATER 5. Splash/lingering-зелья и tipped arrows
  покрыты динамически (`isPotionStack`), цветные кровати/пурпурная плита/варианты
  камня и пр. — выводом из рецептов; творческие/командные предметы (barrier,
  structure block и т.п.) не тегируются, как и в оригинале. Добавлен coverage-тест
  `ConfigAspectsModernVanillaTagCoverageTest`.

## [1.0.10]
### Исправлено
_(набор из PR #7; перенумерован с 1.0.9 → 1.0.10, т.к. номер 1.0.9 занял
влитый в main аудит рендера/локализации)_
- **Иконка нитора (`blockairy`, meta=1) дублировала обычный светящийся куб.**
  У предмета-версии блока не было отдельной модели, поэтому в инвентаре/руке
  вместо текстуры нитора рисовалась текстура meta=0. Добавлена модель
  `models/item/blockairy_nitor.json` (parent `item/generated`, текстура
  `thaumcraft:items/nitor`) и её регистрация в `ClientProxy.setupBlockRenderers()`.
- **Таумометр: экран сканирования дёргался/подёргивался при удержании ПКМ.**
  Причина — две независимые, несинхронизированные попытки определить цель
  скана за тик: (1) полный пересчёт `ScanResult` каждый тик в `onUsingTick` с
  жёстким сравнением через `equals()` (чувствителен к микро-дрожанию наводки —
  меняется даже при взгляде на тот же блок из-за смены fallback-уровня
  `getPickBlock`/`damageDropped`/сырое meta), и (2) независимый пересчёт цели
  каждый **кадр** в `ItemThaumometerRenderer.renderScanReadout()` для HUD.
  Расхождение между этими двумя показывалось как дрожание модели/экрана и сбросы
  звука/частиц. Исправлено: во время активного скана продолжение проверяется по
  лёгкому «ключу цели» (позиция блока/id сущности), а не по полному `ScanResult`;
  рендерер HUD теперь берёт зафиксированный `ItemThaumometer.getActiveScan()`
  вместо повторного поиска цели каждый кадр.
- **Таумометр: по завершении скана, если ПКМ всё ещё зажата, скан мгновенно
  перезапускался** — выглядело как резкий рывок/«скан начинается заново».
  Скан завершается на 5 тиков раньше конца `getMaxItemUseDuration` (принудительный
  `stopActiveHand()`), из-за чего ванильная логика удержания ПКМ сразу же снова
  вызывает `onItemRightClick`, пока кнопка мыши не отпущена. Добавлен флаг
  `awaitingRelease`: после завершения скана новый скан не стартует, пока игрок не
  отпустит ПКМ (проверяется через `CommonProxy.isUseItemKeyDown()`, реализовано в
  `ClientProxy` через `keyBindUseItem.isKeyDown()`).
- Текстуры/модель самого сканера (`scanner.obj`, `scanner.png`, `scanscreen.png`)
  побайтово идентичны оригиналу 4.2.3.5 — расхождений не найдено статическим
  анализом; если после этой правки экран сканирования всё ещё выглядит "криво",
  нужен скриншот/описание конкретно того, что не так, для дальнейшей диагностики.

## [1.0.9]
### Исправлено (таумометр: «Format error» в сообщениях сканирования)
- **Сообщения таумометра начинались с технического текста `Format error:`.**
  `ClientProxy.localizeOrFallback` прогонял шаблоны уведомлений
  (`tc.addaspectpool`, `tc.discoveryerror` и др.) через `I18n.format` без
  аргументов; ванильный `Locale` при `%s`/`%1$s` без аргументов возвращает
  строку `Format error: <шаблон>`, а `%n` молча превращается в перевод строки,
  из-за чего последующий ручной `.replace("%n", ...)` не находил плейсхолдер и
  имя аспекта пропадало (`tc.addaspectdiscovery`). Теперь берётся «сырой»
  перевод через `translateToLocal`, плейсхолдеры подставляются вручную, как и
  задумано. Это же чинит впечатление «нельзя сканировать» у неполноразмерных
  блоков (тростник, головы, стержни Края): их скан доходил до проверки
  родительских аспектов, но вместо нормального «Чтобы понять это, нужно изучить
  …» игрок видел `Format error: …`.
- **Та же ошибка в тултипе Таумономикона:** `GuiResearchBrowser` строил строку
  «Запретное знание (%n)» через `I18n.format`, `%n` превращался в перевод
  строки и уровень запретности исчезал. Переведено на «сырой» перевод.

### Исправлено (локализация)
- **Кольца подмастерья показывали сырой ключ вместо имени.**
  `ItemBaubleBlanks.getItemStackDisplayName` ссылался на старый ключ 1.7.10
  `item.ItemBaubleBlanks.3.name`, а в lang-файлах порта ключ переименован в
  `item.thaumcraft.bauble_blanks.3.name` — код переведён на новый ключ.
- **Добавлены ключи, которые код использует, но при портировании они потерялись**
  (в обоих языках): названия зачарований `enchantment.frugal/potency/haste/`
  `repair/wand_fortune` (без них тултип зачарования показывает сырой ключ),
  заголовок GUI `container.alchemyfurnace`, имена жидкостей `fluid.fluidPure/`
  `fluidDeath/fluxgoo/fluxgas`, сообщения смерти `death.attack.taint/tentacle/`
  `swarm/dissolve` (+варианты), тултипы `tc.adv`, `tc.discount`, `tc.markedloc`.
  Английские значения взяты из оригинального `en_US.lang` 4.2.3.5.

### Исправлено (сверка рендера с оригинальным Thaumcraft 4.2.3.5 по байткоду)
- **Нитор рендерился как светящийся куб (`animatedglow`) вместо красного огонька.**
  В оригинале `BlockAiry.getRenderType()` возвращает −1 для всех метаданных —
  блок нитора вообще не рисуется, весь визуал даёт `TileNitor` (красные/жёлтые
  wispFX-частицы, сверено с оригиналом 1-в-1). В порту мета 1 (нитор) попадала в
  ветку `MODEL` и рисовалась кубом. Теперь мета 1 добавлена в `INVISIBLE`.
- **Волшебный лес: изумрудная трава/листва.** Цвета в
  `BiomeMagicalForest` были искажены при переносе: трава `0x66FF05` → `0x66FFC5`,
  листва `0x55FF11` → `0x55FF81` (оригинальные константы 6750149 и 5635969 из
  `BiomeGenMagicalForest.func_150571_c/func_150558_b`). Также были перепутаны
  температура и влажность (в оригинале `setTemperatureRainfall(0.7, 0.6)`,
  в порту было 0.6/0.7) — поправлено.
- **Тейнт-биом: перепутаны местами цвета травы и листвы.** В оригинале листва
  `0x6D4189`, трава `0x7C6D87`; в порту было наоборот.
- **Eerie-биом: опечатки в двух цветах.** Трава `0x405040` → `0x405340`,
  вода `0x2E52DF` → `0x2E535F` (оригинал: 4215616 и 3035999).
- **Броня Fortress и пустотная роба: неверный выбор толщины модели по слоту.**
  `getArmorModel` сравнивал `EntityEquipmentSlot.ordinal()` (OFFHAND=1, LEGS=3)
  с константами 1.7.10 (`1` = нагрудник, `3` = ботинки). В итоге штаны получали
  толстую модель (1.0f), а нагрудник/шлем/ботинки — тонкую (0.5f). Теперь слот
  сравнивается напрямую: CHEST/FEET → 1.0f, HEAD/LEGS → 0.5f, как в оригинале
  (`ItemFortressArmor`, `ItemVoidRobeArmor`; культистская броня была портирована
  верно и не менялась).

## [1.0.8]
### Исправлено (правка по итогам ревью 1.0.7)
- **Подпись `tc.aspect.primal` не убрана, а переведена.** В 1.0.7 подпись у
  первичных аспектов была удалена целиком — по уточнению должна остаться, просто
  как нормальный текст. Добавлен перевод (`tc.aspect.primal=Первичный аспект` /
  `Primal Aspect`) в `ru_ru.lang` и `en_us.lang`, отрисовка строки в
  `drawAspectPage` восстановлена.
- **Подсветка при наведении: прямоугольник заменён на кружок вокруг иконки
  аспекта.** В 1.0.7 затемнялась вся плитка (иконка + подпись) прямоугольником —
  по уточнению нужен именно кружок вокруг иконки. Добавлен метод
  `drawHoverRing` (кольцо через `GL_TRIANGLE_STRIP`, полупрозрачное золотистое),
  вызывается вместо `drawRect`, центрируется на 32×32-иконке аспекта.
- Добавлен `KNOWN_ISSUES.md` — список открытых багов/недоделок порта
  (перенесён из внутреннего таск-трекера сессии), чтобы держать его в репозитории,
  а не только в истории чата.

## [1.0.7]
### Исправлено
- **Таумонимикон, раздел «Аспекты»: у первичных аспектов отображалась техническая
  подпись `tc.aspect.primal`** (`GuiResearchRecipe.drawAspectPage`). У ключа не было
  перевода ни в одном lang-файле, поэтому в игре рисовался сырой строковый литерал
  вместо текста. По просьбе — подпись убрана целиком (не переведена, а удалена):
  для первичных аспектов теперь просто не рисуется вторая строка под иконкой,
  как и для составных аспектов не рисовалась их разбивка на компоненты.
- **Таумонимикон: наведение на аспект в разделе «Аспекты» никак не подсвечивалось.**
  В оригинале плитка аспекта под курсором подсвечивается затемняющим прямоугольником
  (`_back.png`), в порту после восстановления страницы «Аспекты» (1.0.6) этого не
  было — непонятно, какой именно аспект выбран. Добавлено полупрозрачное затемнение
  (`drawRect`, alpha 0x30) на всю плитку (иконка + подпись) при наведении, до отрисовки
  содержимого плитки.
- **Голову дракона (и любые головы мобов) нельзя было просканировать таумометром,
  когда она стоит в мире как блок.** Диагностика показала, что это не баг
  рейтрейса/хитбокса (ванильный `getBoundingBox`/`collisionRayTrace` корректно
  целится в блоки-черепа, `Item.rayTrace(world, player, true)` в порту — тоже), а
  отсутствие регистрации аспектов: `ConfigAspects` не содержал **ни одной** записи
  для `Items.SKULL` — и в порту, и (проверено по декомпилированному источнику)
  в самом оригинале TC4 4.2.3.5 (черепа не использовались в крафте 1.7.10-эпохи,
  голова дракона появилась только в 1.9). Без аспектов `ItemThaumometer.findRawScanTarget`
  отбрасывает любой кандидат с нулевым списком аспектов — сканирование молча
  ничего не делает, что выглядело как «нельзя даже навестись». Добавлены аспекты
  для всех 6 вариантов метаданных `Items.SKULL` (скелет, иссохший скелет, зомби,
  игрок, крипер, дракон), уровень по реальной сложности добычи в ванили — от
  обычного дропа скелета до эксклюзивной награды за убийство Дракона Края.

## [1.0.6]
### Исправлено
- **Таумонимикон, раздел «Аспекты»: страница с известными аспектами была пустой**
  (`GuiResearchRecipe.java`). Исследование `ASPECTS` в порту содержит только 3
  статичные текстовые страницы (`ConfigResearchBasics`) — в оригинале 1.7.10
  конструктор `GuiResearchRecipe` при `research.key.equals("ASPECTS")` **динамически
  дописывал** к ним ещё страницы (`ResearchPage(AspectList)`, по 4 аспекта на
  страницу) из списка аспектов, известных игроку
  (`IPlayerKnowledge.getAspectsDiscovered()`). Этот блок в порту отсутствовал
  целиком — страница со знаниями игрока никогда не создавалась. Восстановлено:
  добавлен метод `buildAspectsPages`, использующий уже существующий в порту
  клиент-безопасный путь `CommonProxy.getPlayerKnowledge(player).getAspectsDiscovered()`
  (тот же паттерн, что и в `GuiResearchTable`).
- **Таумонимикон: наведение на аспект не показывало предметы с этим аспектом**
  (второй симптом того же бага, тоже отсутствовал в порту целиком). В оригинале
  наведение на плитку аспекта в разделе «Аспекты» рисует сетку иконок предметов,
  которые игрок просканировал и у которых есть этот аспект — построено из личной
  истории сканирований (`aspectItems: HashMap<Aspect, ArrayList<ItemStack>>`).
  Проблема архитектурная: порт хранит просканированные предметы как
  **необратимый хэш** (`ScanManager.generateItemHash`, строка вида `"@12345"`) —
  в отличие от оригинала, у порта не было способа превратить хэш обратно в
  `ItemStack`. Оригинал решал это фоновым потоком `MappingThread`, который один
  раз проходит по всем зарегистрированным предметам (включая под-варианты:
  цвета шерсти, зелья и т.п.) и складывает `хэш → ItemStack` в статичный кэш
  (`GuiResearchRecipe.cache`). Перенесено байт-в-байт по духу, адаптировано под
  1.12.2 (`Item.REGISTRY` + `getSubItems(CreativeTabs.SEARCH, ...)` вместо
  унифицированных числовых ID из 1.7.10): новый `MappingThread.java`, кэш и
  `putToCache`/`getFromCache` в `GuiResearchRecipe`, запуск потока лениво из
  `ClientTickEventsFML.playerTick` (один раз за сессию, как в оригинале).
  `drawAspectPage` дополнен вторым проходом, рисующим сетку иконок предметов при
  наведении на аспект — если у аспекта нет просканированных предметов, остаётся
  текстовая подсказка (имя + описание), которой в оригинале не было, но которая
  не мешает и покрывает случай «аспект известен, но ни один предмет с ним ещё не
  просканирован».

## [1.0.5]
### Добавлено
- **Аспекты для ванильных блоков/предметов 1.8–1.12.2** (новый метод
  `ConfigAspects.registerModernVanilla`). Без явного тега (или тега, выведенного
  из рецепта) у объекта нет аспектов, и `ScanManager.validScan` отклоняет
  сканирование — таумометр «не может просканировать» такой предмет. Покрыты
  сырые дропы и некрафтимые предметы: призмарин/морской фонарь, красный
  песчаник, кролик/баранина (мясо, лапка, шкура), железный люк, знамя, эндер
  (хорус, пурпур, эндовый стержень/кирпичи, дыхание дракона, кристалл Края),
  элитры, щит, спектральные/зачарованные стрелы, свёкла (+суп/семена), лодки из
  разных пород, магма-блок, блок адского нароста, красный адский кирпич, костяной
  блок, наблюдатель, все 16 шалкеров + панцирь шалкера, тотем бессмертия,
  железный самородок, книга знаний, тропинка, бетон/бетонная пыль, все 16
  глазурованной керамики.
  Важно: эти предметы появились в ваниле уже после TC4 4.2.3.5 (1.7.10), поэтому
  в оригинале и на вики (ru.minecraft.wiki/w/Thaumcraft_4/Аспекты) для них
  просто нет эталонных значений — цифры подобраны по аналогии с похожими
  зарегистрированными объектами (не байт-в-байт, в отличие от тростника ниже).
  Если что-то ощущается разбалансированным — пишите, поправим.
### Исправлено
- **Полный аудит ванильных аспектов** (`registerVanillaBlocks`, `registerVanillaItems`,
  `registerOreDictionary` в `ConfigAspects.java`): значения сверены байт-в-байт с
  задеобфусцированным декомпилем оригинального `thaumcraft/common/config/ConfigAspects.java`
  из TC4 4.2.3.5 (1.7.10), а не с вики (вики оказалась неполной/недостоверной —
  см. пункт про тростник ниже). Обнаружилось, что почти весь блок регистрации
  ванильных блоков/предметов был подобран «по аналогии», а не перенесён из
  оригинала — это баланс-баг, задевающий десятки предметов. Самые заметные
  исправления: шерсть (была PLANT2+TOOL1 через выдуманный oreDict-тег
  `"blockWool"` → CLOTH 4 + CRAFT 1, прямой тег на `Blocks.WOOL`), стекло (был
  выдуманный oreDict-тег `"blockGlass"`=CRYSTAL2 → CRYSTAL 1, прямой тег на
  `Blocks.GLASS`), эндер-жемчуг (VOID4+TRAVEL2 → ELDRITCH 4 + MAGIC 2 + TRAVEL 4),
  порох (FIRE2+ENERGY2+ENTROPY1 → FIRE 4 + ENTROPY 4), ведро (METAL3+VOID1 →
  METAL 8 + VOID 1), золотой слиток (METAL6 → METAL 3 + GREED 2), обсидиан
  (EARTH4+FIRE2+DARKNESS2 → EARTH 2 + FIRE 2 + DARKNESS 1), приготовленные
  мясные продукты (были BEAST+LIFE → CRAFT 1 + FLESH + HUNGER по образцу
  оригинала), кожа, кость, гнилая плоть, глаз паука, стержень ифрита, слеза
  гаста, слизневый шарик, книга/бумага (теперь `registerComplexObjectTag`,
  как в оригинале) и другие. Полный список — в диффе `ConfigAspects.java`.
  Также добавлены отсутствовавшие в порту прямые теги (bedrock, глазурованная
  сталь и т.д.) и убраны две выдуманные oreDict-записи, которых нет в
  оригинале (`"blockGlass"`, `"blockWool"`).
- **Баланс аспектов для контента 1.8–1.12.2** (`registerModernVanilla`, для
  этих предметов эталона в оригинале TC4 4.2.3.5 нет — только 1.7.10-контент):
  числовые значения аспектов пересчитаны по сложности добывания предмета, а
  не проставлены произвольно. Общее правило: чем труднее/опаснее получить
  предмет (редкий дроп с боем, эксклюзив измерения Края, структура с охраной,
  Silk Touch-зависимость), тем выше суммарный вес аспектов, и наоборот —
  тривиальные крафты (конкрит, глазурованная терракота, лодки, тропинка)
  остаются низкими. Заметные пересмотры: элитры и панцирь шалкера (лут
  Края/Энд-корабля — редчайшие предметы в ванили) подняты до максимального
  тира; тотем бессмертия (редчайший дроп иллюзиониста-эвокера) — тоже топ-тир;
  дыхание дракона (доступно только после боя с драконом Края) поднято
  значительно; лапка кролика (~10% дроп) поднята за редкость; наросшая
  адская бородавка (требует Silk Touch, некрафтима) поднята; наблюдатель
  (требует незер-кварц) немного поднят; тривиальные крафты (грязевая
  тропинка, конкрит) немного снижены. Приготовленные кролик/баранина
  переведены на схему `Craft+Flesh+Hunger`, как остальные приготовленные
  мясные продукты выше.
- **Тростник (сахарный тростник) не сканировался**: сырой дроп без рецепта-
  источника не получал аспектов → `validScan` отклонял. Явно зарегистрирован
  `Items.REEDS`. Источник истины — декомпилированный оригинал (не вики, которая
  для этого предмета оказалась неполной): **Plant 1 + Water 1 + Air 1**
  (байт-в-байт с оригиналом).
- **Таумометр: брошенный предмет стоял вертикально** (задача #23). Дисплей-
  трансформация `ground` в `itemthaumometer_tesr.json` задавала поворот `[90,0,0]`,
  ставивший диск сканера вертикально. Заменено на `[0,0,0]` — диск лежит плашмя
  лицом вверх (у модели дефолтная ориентация «лицом вверх»; поворот `[90,0,0]`
  в `gui` — это наклон к камере, а не к земле).

## [1.0.4]
### Исправлено
- **Матрица наполнения не активировалась (клик жезлом ничего не давал)**: настоящая
  причина сборки алтаря. `TileInfusionMatrix.onWandRightClick` в порту начинался с
  `if (world.isRemote) return 0;` — на клиенте возвращался `0`, из-за чего
  `ItemWandCasting.onItemUseFirst` (`ret >= 0` → `return PASS`) **не доходил** до
  реестра триггеров жезла (`WandTriggerRegistry`, событие 3 → `createInfusionAltar`).
  Путь сборки был мёртв целиком. В оригинале TC4 обе ветки гейтятся `!world.isRemote`,
  а на клиенте метод возвращает `-1`, пропуская клик к триггеру. Восстановлено
  байт-в-байт. Теперь клик жезлом по рунической матрице собранной структуры
  (пьедестал + 4 столба из магического камня/кирпича) активирует алтарь.
  Напоминание: сборка гейтится исследованием `INFUSION` и требует по 25 вис
  каждого из 6 первичных аспектов в жезле.

## [1.0.3]
### Исправлено
- **Пустотная роба (комплект)**: нагрудник и поножи в инвентаре отображались
  неправильно — item-модели использовали только верхний слой-оверлей
  (`voidrobechest`/`voidrobelegs`), без основной текстуры. Переведены на
  двухслойную композицию как в оригинале TC4 (`func_77618_c`):
  слой0 = база (`voidrobechestover`/`voidrobelegsover`), слой1 = оверлей
  (`voidrobechest`/`voidrobelegs`). Капюшон и ботинки не менялись (корректны).
- **Светящийся заросший камень** (`blockEldritch` meta 4, «glowing crusted
  stone»; заодно meta 5 — рудный вариант): в мире был почти невидим — модель
  задавала уменьшенный вложенный бокс (2..14) вместо полного куба. Переведены
  на `block/cube_all` с аутентичной анимированной текстурой `es_i_1`/`es_i_2` —
  полноценный видимый блок со свечением и коллизией.
- **Ваза (loot urn)**: была сплошной коробкой. Модель переделана в силуэт вазы —
  тело + вытянутое вверх узкое горлышко; корректно отображается и в мире, и как
  предмет (трансформации предмета наследуются от `block/block`).
- **Руническая матрица (матрица наполнения)**: до активации крутилась/анимировалась
  как готовый алтарь — из-за чего структуру «нельзя было собрать/опознать». Причина:
  в `TileRunicMatrixRenderer` поле `startUp` принудительно поднималось до `1.0F`
  (`tile.startUp <= 0 ? 1.0F : ...`), из-за чего вращение TESR применялось всегда.
  В оригинале TC4 используется сырое `startUp` (= `0.0F` пока `!active`): матрица —
  **статичный цельный блок** до активации и обретает вращение/анимацию только после.
  Исправлено на байт-в-байт поведение оригинала.

## [1.0.2]
### Исправлено
- **Адская печь**: «злое лицо» больше не перекрывается огнём. Порядок глубин
  в `ArcaneFurnaceBakedModel.addNozzleQuads` был перевёрнут (огонь рендерился
  спереди) — теперь решётка спереди, лицо посередине, огонь сзади.
- **Сумка для набалдашников (бижутерия/пояс)**: не было иконки — текстура
  `focuspouchbauble.png` была пустой; модель переведена на рабочую `focuspouch`.
- **Ящики и вазы (loot crate/urn)**: модели использовали `block/cube` (полный
  куб) при `isOpaqueCube=false` и уменьшенном AABB — грани утыкались в соседние
  блоки и рендерились чёрными. Переделаны на корректную геометрию-коробку
  (шаблоны `block/loot_urn`, `block/loot_crate`), исправляет вид в мире и в креативе.

### Известные проблемы / в работе
- Боевая таумиевая броня и пустотная роба мага: розовый рендер на модели игрока
  (текстуры байт-в-байт совпадают с оригиналом TC4 — требуется отладка в игре).
- Таумометр: брошенный предмет стоит вертикально, должен лежать горизонтально.
- Нитор: текстура идентична оригиналу TC4 (аутентичная) — уточнить ожидание.
- Волшебный биом: цветовая гамма (светло-изумрудная трава/листва).
- Исследования для новых блоков/предметов, добавленных после 1.0.

## [1.0.1]
### Исправлено
- **Адская печь**: восстановлена решётка «злого лица» (инверсия `getNozzleFacing`).
- **Робы тауматурга**: иконки рендерились серыми — восстановлено тонирование
  крашеной кожаной брони (двухслойные модели + `IItemColor`).

## [1.0.0]
- Первый играбельный порт на базе FOREVA.
