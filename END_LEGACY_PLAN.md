# «Наследие Края» — новый контент порта (End Legacy)

**Решение владельца, 2026-08-02.** Первый неоригинальный контент репозитория:
элитры, Край, шалкеры, обереги. **Правило «1 в 1 с оригиналом» здесь не
действует** — оригинала не существует. Действует другое: стиль, стоимость и
голос — как у TC4; баланс калибруется по числам существующих рецептов; тексты
пишутся от лица автора Таумономикона. Аудитам: не «чинить» этот модуль по
отсутствующему оригиналу.

Выбор владельца: крылья — **настаиванием в нагрудник** (не бижутерия-слот).
Реализация: **инфузионные зачарования** — как `enchRepair`/`enchHaste`, потому
что одно зачарование ложится на *любой* нагрудник без вариантов предмета.
Исследования — во **вкладках самого TC4** (Артифис, Эльдрич), не в отдельной.

---

## 1. Дерево исследований

### ARTIFICE (Артифис)

| Ключ | Название | Иконка | Родители | Как открывается |
| --- | --- | --- | --- | --- |
| `SOARING` | Парящие чары / Soaring Infusion | `ItemStack(Items.ELYTRA)` | `INFUSIONENCHANTMENT` | скрытое, триггер — скан элитры (`setHidden().setItemTriggers(элитра)`) |
| `ASCENSION` | Вознесение / Ascension | `ItemStack(Items.DRAGON_BREATH)` | `SOARING` + скрытый `DRACONIC_SECRETS` | обычное, видно после обоих родителей |

### ELDRITCH (Эльдрич)

| Ключ | Название | Иконка | Родители | Как открывается |
| --- | --- | --- | --- | --- |
| `DRACONIC_SECRETS` | Драконья живучесть / Draconic Vitality | `ItemStack(Items.DRAGON_BREATH)` | `ELDRITCHMINOR` (скрытый) | скрытое, триггер — скан драконьего дыхания или яйца дракона |
| `END_SPIRES` | Шпили Извне / Spires from Beyond | `blockCosmeticSolid:11` (древний камень) | `DRACONIC_SECRETS` | фаза 3, вместе с данжами |
| `WARD_ARROWS` | Оберег отклонения / Ward of Deflection | заготовка амулета | `INFUSION` (ARTIFICE) | фаза 2 |
| `WARD_LASTBREATH` | Оберег последнего вздоха / Ward of the Last Breath | тотем бессмертия | `WARD_ARROWS` + `DRACONIC_SECRETS` | фаза 2 |

Прогрессия по замыслу: элитра из корабля Края (ванильный гейт редкости) →
Парящие чары → скан дыхания дракона (значит, игрок убил дракона) → Драконья
живучесть → Вознесение и Оберег последнего вздоха. Дракон — узел, на который
завязан весь верх ветки, как просил владелец.

## 2. Предметы и атрибуты

### Зачарование «Парящие чары» (`enchSoaring`, нагрудник, ур. 1)

Планирование. В воздухе, при падении, не крадучись и не в жидкости:
- скорость падения ограничивается `motionY ≥ -0.12`;
- дрейф вперёд по взгляду `+0.035/тик` к горизонтальной скорости;
- `fallDistance` сбрасывается (мягкая посадка).
Ничего не потребляет. По ощущению — параплан, слабее ванильной элитры на
разгоне, но не требует высоты и работает поверх любой брони.

### Зачарование «Вознесение» (`enchAscension`, нагрудник, ур. 1)

То, ради чего всё: **взлёт с земли и тяга без фейерверков**. Включает
планирование. Дополнительно:
- **взлёт**: прыжок с земли с зажатым прыжком — вертикальный импульс `+0.9`;
- **тяга**: удержание прыжка в воздухе — ускорение вдоль взгляда `0.06/тик`,
  потолок скорости `1.6`;
- **топливо**: 1 очко Aer (100 единиц) за 25 тиков тяги из любого жезла в
  инвентаре; нет виса — тяга гаснет, планирование остаётся.
Ввод: клиент читает зажатый прыжок и шлёт `PacketSoaringThrust` при смене
состояния (образец — `PacketFlyToServer` ховер-ранца).

### Крылья на спине

`ModelWings` уже в порту (роба стратосферы). Рендер-слой через
`RenderPlayerEvent.Post`: если на нагруднике Парящие чары/Вознесение и это не
роба стратосферы — рисуем крылья. Текстура — та же, что у робы (вырезается из
`ichorGem1.png`); для Парящих чар без KAMI — перекраска в нейтральный тон
(фаза 1: та же текстура, отдельная — по итогам взгляда в игре).

### Оберег отклонения (фаза 2; слот CHARM, на базе заготовки амулета)

- Снаряды (стрелы, трезубцы, снежки, шалкерные пули): урон отменяется,
  снаряд отражается; 250 прочности, 1 за отражение; чинится инфузией Repair.
- Крафт-цепочка через алхимию (см. §4).

### Оберег последнего вздоха (фаза 2; слот CHARM)

Ответ владельца на «у нас же есть тотем бессмертия» — наш вариант, из слота
бижутерии, руки не занимает:
- смертельный урон → отмена, 3 сердца, Сопротивление II и Огнестойкость 40с
  (как у тотема), **+2 временного варпа** — обман смерти не бесплатен;
- оберег рассыпается в «Треснувший оберег» — его можно настоять заново
  (дешевле, чем первый крафт: душа уже приучена).

## 3. Данжи Края (фаза 3 — эскиз, имплементация отдельным заходом)

«Шпили Извне» — эльдричьи обелиски на внешних островах Края (не пересекаются
с городами Края): древний камень (`blockCosmeticSolid:11-13`), эльдричьи
блоки, взаперти — эльдричий страж. Лут: осколки знаний (очки ELDRITCH/ALIENIS),
инфузионные компоненты, редко — праймал-заряд. Родные генераторы-образцы:
`WorldGenEldritchRing`, `GenBossRoom` (измерение Далёких земель уже умеет
комнаты). Исследование `END_SPIRES` подсвечивает их существование и лут.
Дизайн-вопросы (решить перед фазой 3): плотность на островах, глубина лута,
не дублирует ли Далёкие земли — цель, чтобы Край читался как «второе лицо»
Извне, а не копия.

## 4. Рецепты

Калибровка — по соседям из `ConfigRecipesInfusionSlice` (Repair: неста-
бильность 4, MAGIC 8 + CRAFT 10 + ORDER 10; Haste: 3, MAGIC 4 + TRAVEL 8 +
FLIGHT 8).

**Парящие чары** (инфузионное зачарование, гейт `SOARING`):
- нестабильность 4; эссенция: `VOLATUS 24, AER 16, MAGIC 8`;
- компоненты: элитра, перо ×2, зачарованная ткань (`itemResource:14`).

**Вознесение** (гейт `ASCENSION`):
- нестабильность 6; эссенция: `ALIENIS 24, VOLATUS 24, POTENTIA 16, MAGIC 8`;
- компоненты: драконье дыхание, панцирь шалкера, стержень Края,
  зачарованная ткань.

**Алхимия** (тигель, фаза 2):
- «Вытяжка пустоты» из хорус-плода: `VACUOS 4 + ALIENIS 2` → компонент
  оберегов;
- заготовка оберега = заготовка амулета + вытяжка пустоты (верстак).

**Оберег отклонения** (инфузия, гейт `WARD_ARROWS`): нестабильность 3;
`MOTUS 16, ARMOR 12, AER 8`; заготовка оберега, панцирь шалкера, щит,
стрела ×2.

**Оберег последнего вздоха** (инфузия, гейт `WARD_LASTBREATH`):
нестабильность 8; `VICTUS 32, ALIENIS 16, PRAECANTATIO 16, ARMOR 8`;
компоненты: **тотем бессмертия**, оберег отклонения, драконье дыхание,
слиток ихория? — нет: без KAMI-зависимости, вместо ихория — праймал-заряд
(`itemResource:15`?) — уточнить по фактическому списку ресурсов при
имплементации фазы 2.

## 5. Тексты Таумономикона

Голос автора TC4: первое лицо, наблюдение → вывод → предостережение.
Полные тексты EN+RU — в lang-файлах (§ имплементации); опорные фрагменты:

**SOARING.lore:** en «The sky was never locked» / ru «Небо никогда не было
заперто».

**SOARING, стр. 0 (ru):** «Крылья, что я снял с останков корабля в Крае, —
не одежда и не механизм. Это застывший росчерк чужой воли, помнящий полёт.
Настояв их сущность на нагруднике, я убедился: помнить полёт может любая
броня. Падение теперь — лишь медленный шаг вперёд.»

**DRACONIC_SECRETS.lore:** en «It refused to die politely» / ru «Он отказался
умирать вежливо».

**DRACONIC_SECRETS, стр. 0 (ru):** «Дыхание погибшего дракона не рассеялось.
Оно висело в воздухе Края, как будто смерть для этого существа — вопрос
договорённости, а не факта. Я собрал его в склянку. Склянка тёплая. Мне
следовало бы насторожиться ещё тогда.»

**ASCENSION, стр. 0 (ru):** «Планирование — это падение, растянутое в
удовольствие. Но дыхание дракона знает, как падать вверх. Вплетя его в
парящие чары, я оттолкнулся от земли — и земля отпустила. Аэр в жезле тает,
пока я держусь в небе, и это честная цена: небо не обязано быть бесплатным.»

**WARD_LASTBREATH, стр. 0 (ru):** «Тотем требует руки. Но умирающий редко
успевает выбрать, что у него в руке. Оберег на шее не спрашивает разрешения —
он просто отказывает смерти от моего имени. После каждого отказа по вискам
течёт холод, и я не уверен, что это пройдёт.» (варп +2 — в тексте намёком.)

## 6. Порядок имплементации

1. **Фаза 1 (этот заход): крылья целиком.** `EnchantmentSoaring`,
   `EnchantmentAscension`, физика (чистые функции + тонкий обработчик),
   `PacketSoaringThrust` (в хвост реестра + `REFERENCE_PACKET_COUNT`),
   рендер крыльев, исследования SOARING/ASCENSION/DRACONIC_SECRETS, рецепты,
   lang EN+RU, guard + рантайм-тест физики.
2. **Фаза 2: обереги** (+ алхимия вытяжки, слот CHARM свободен).
3. **Фаза 3: Шпили Извне** (+ `END_SPIRES`).

Пакеты: `common/lib/endgame/` для обработчиков, конфиг — `ConfigEndLegacy`,
исследования — в существующие `ConfigResearchArtifice`/`ConfigResearchEldritch`
отдельными блоками с пометкой «End Legacy — новый контент».
