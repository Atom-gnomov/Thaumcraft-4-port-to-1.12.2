# План интеграции Thaumic Tinkerer

Рабочий документ по добавлению контента **Thaumic Tinkerer** (авторы pixlepix,
nekosune, изначально Vazkii) в порт Thaumcraft 4 → 1.12.2.

Читать вместе с `PORTING_HANDOFF.md` (adoption FOREVA) — это отдельное,
параллельное направление работ. Покрытие по классам — в `TT_PORT_AUDIT.md`.

---

## СТАТУС на 1.1.3.0 (2026-07-25)

**Готово и лежит в jar:** 7 фокусов палочки · 4 утилитарных предмета ·
тёмный кварц (3 варианта) · воронка · магнит · ремонтник ·
трансвекторный интерфейс + соединитель + дислокатор · планшет динамизма ·
осмотический чарователь.
Всё крафтится в выживании, локализовано на en + ru, покрыто guard-тестом
`ThaumicTinkererFociStaticGuardTest`.

**Фазы:** Фаза 1 (функциональные тайл-блоки) — ✅ завершена.
Фаза 2 (трансвекторы) — ✅ завершена.
Фаза 3 (автоматизация и зачарование) — ✅ завершена.
**Следующее — 14 своих зачарований TT** (реестр расхождений, п. 1), затем
Фаза 4: прочие блоки (платформа, RPlacer, камуфляж, анализатор аспектов,
силовое поле, газовые).

**Осталось до полного TT** (крупными мазками, детали в разделе «Фазы»):
прочие блоки
(платформа, RPlacer, камуфляж, анализатор аспектов, силовое поле, газовые) →
предметы (шлем прозрения, ткань заклинаний, чернильница, семена/зерно,
аспекты мобов, яркий нитор, форма душ, книга обмена) → своя ветка исследований
в Таумономиконе вместо гейта за чужими.
Плюс KAMI, своё измерение TT и опциональные интеграции — см. «Пересмотр
«вне области»». Ничего не выбрасываем.

**Расхождения с оригиналом в уже сделанном: 12 пунктов, закрыто 6**, см. «Реестр
расхождений». Пп. 1–4 (14 своих зачарований TT, магнит мобов, камуфляж
трансвектора, TESR планшета) известны раньше. Пп. 5–9 найдены ревизией:
заходы 1.0.42–1.0.45 делались без исходника оригинала, и почти всё в них
разошлось — все 7 фокусов (канальные вместо мгновенных, другие стоимости,
Deflect и Dislocation вообще про другое), Placement Mirror и Cat Amulet не в
том тире (KAMI), талисманы не Baubles, воронка — другое устройство, у тёмного
кварца нет плиты и ступеней. Это долг, а не «наш вариант».

**Что мешает работе прямо сейчас:** см. «Открытый долг репозитория» ниже —
5 падающих guard-тестов рендера и непереносимый `gradle.properties`.
Оба к TT отношения не имеют, но найдены в ходе этих работ.

**Сборка:** нужен JDK 8 (Gradle 4.10.3 + ForgeGradle 2.3 не работают на новых
Java). Команда, обходящая жёсткий путь в `gradle.properties`:

```
cd mod && ./gradlew.bat build -x test -Dorg.gradle.java.home=<путь-к-jdk8>
```

---

## Подход

Оригинальный TT 1.7.10 — аддон к TC4 на 301 собственный java-файл. Его jar
несовместим с 1.12.2 (Forge API изменился полностью), поэтому **мы не портируем
jar, а реализуем фичи заново** нативно под 1.12.2 поверх нашего API, беря из
оригинала поведение, баланс и текстуры (с атрибуцией авторам).

Источник эталона: `Thaumic-Tinkerer/ThaumicTinkerer`, ветка `1.7.10`.
Клонировать при необходимости:
`git clone --depth 1 -b 1.7.10 https://github.com/Thaumic-Tinkerer/ThaumicTinkerer <tmp>`

### Почему не отдельным модом
Контент живёт внутри нашего мода отдельным модулем — так не нужен второй
mod-id, второй жизненный цикл и API-мост между модами. Изоляция достигается
раскладкой пакетов и отдельным реестром рецептов.

---

## Соглашения модуля (соблюдать в каждом заходе)

**Раскладка пакетов** — TT-контент всегда в своих подпакетах, чтобы он не
смешивался с корпусом TC4-парити:
- `thaumcraft/common/blocks/tinkerer/`
- `thaumcraft/common/items/tinkerer/`
- `thaumcraft/common/tiles/tinkerer/`
- фокусы — исключение: лежат рядом с нашими в `common/items/wands/foci/`
  (реализуют общий `ItemFocusBasic`)

**Рецепты — только в `ConfigTinkerer`.** Не в `common/config/recipes/`: там
корпус TC4-парити, по которому идут аудиты соответствия оригиналу. TT — новый,
непаритетный контент. Вызывается из `ConfigRecipes` на init.

**Гейт исследованиями.** Новых веток в Таумономиконе пока не заводим — каждый
предмет вешается на тематически близкое *существующее* исследование TC4
(например фокус переплавки → `FOCUSFIRE`). Своя ветка исследований TT — задача
отдельной фазы (см. ниже).

**Guard-тест.** Один общий `ThaumicTinkererFociStaticGuardTest` (несмотря на имя
— покрывает весь модуль). Расширяется в каждом заходе: регистрация, рецепт,
модели, текстуры, lang.

**Релиз каждого захода:** bump `version` в `mod/build.gradle` → запись в
`CHANGELOG.md` сверху → `./gradlew test` зелёный → коммит `feat(tinkerer): ...
— x.y.z`.

**Атрибуция.** В javadoc каждого класса — ссылка на оригинал и авторов.

---

## Сделано (1.0.42 – 1.0.53)

| Версия | Содержимое |
| --- | --- |
| 1.0.42 | 7 фокусов палочки: Переплавка, Телекинез, Полёт, Лечение, Отражение, Дислокация, Эндер-сундук |
| 1.0.43 | 4 утилитарных предмета: Зеркало установки, Талисман очищения, Талисман опыта, Кошачий амулет |
| 1.0.44 | Тёмный кварц — декор-блок, 3 варианта (обычный/резной/колонна) |
| 1.0.45 | Воронка — первый функциональный тайл-блок (сбор дропов в инвентарь снизу) |
| 1.0.46 | Магнит + закрытие долгов модуля: русская локализация всего TT, рецепты для кварца и воронки |
| 1.0.47 | Ремонтник — первый TT-блок на эссенции (тяга из трубы, TOOL/CRAFT/ORDER → 8/5/3 прочности) |
| 1.0.48 | Трансвекторный интерфейс + соединитель — подмена блока в радиусе 4 через capability |
| 1.0.49 | Трансвекторный дислокатор — обмен блоками в радиусе 16 по импульсу редстоуна |
| 1.0.50 | Планшет динамизма — работает инструментом по блоку перед собой через fake-player |
| 1.0.51 | Осмотический чарователь — зачарование за вис из жезла, многоблок из 6 столбов |
| 1.0.52 | Настоящий GUI планшета динамизма (контейнер + экран + кнопки), как в оригинале |
| 1.0.53 | Настоящий экран чарователя: сетка предложений, уровни, полосы стоимости |

Валидированы пайплайны: предмет, фокус, блок с метаданными, блок с тайлом и
тик-логикой, блок с направлением + инвентарём + потреблением эссенции,
блок со связью на чужую позицию (capability-делегирование, перенос тайлов).

---

## Закрытые долги

- ~~**Нет русской локализации TT-контента**~~ — закрыто в 1.0.46: добавлены все
  18 ключей, `en_us.lang` и `ru_ru.lang` снова синхронны.
- ~~**Тёмный кварц и воронка не крафтились**~~ (были только в креативе, рецептов
  не существовало) — закрыто в 1.0.46.

## Открытый долг репозитория (не TT, но мешает)

- **5 guard-тестов клиентского рендера падают** (ArcaneFurnace, ClientProxy
  beam/bolt, Infusion, VisEnergy, item-model routing). Проверено через stash:
  падали и до работ по TT — наследие adoption рендереров FOREVA 1.0.36–1.0.45.
  `PORTING_HANDOFF.md` при этом утверждает, что набор зелёный.
- **`mod/gradle.properties` в гите с абсолютным путём чужой машины**
  (`org.gradle.java.home=C:/Users/Game-On-Dp/...`) — сборка падает у всех
  остальных. Обходится через `-Dorg.gradle.java.home=<путь>`.

---

## Фазы

### Фаза 1 — функциональные тайл-блоки ✅ завершена

- **1.0.46 — Магнит** ✅ Радиус `сигнал / 2` и скорость 0.25 как в оригинале,
  ПКМ переключает притягивание ⇄ отталкивание.
- **1.0.47 — Ремонтник** ✅ Тянет 1 эссенцию из трубы со стороны `facing`
  раз в 10 тиков; `TOOL`/`CRAFT`/`ORDER` → 8/5/3 прочности; объявляет тягу
  `TOOL` силой 128. Слот открыт для воронок/труб через `IItemHandler`, при
  ломании блока инструмент выпадает. Ветка TConstruct из оригинала не портирована.

### Фаза 2 — трансвекторы (флагман TT) ✅ завершена

- **1.0.48 — Трансвекторный интерфейс + соединитель** ✅
  Радиус 4 по осям, делегирование через `getCapability` (предметы, жидкости,
  энергия любых модов) + явное делегирование `IAspectContainer`/
  `IEssentiaTransport`. Связывание в два клика соединителем. Незагруженный
  чанк не форсится, связь сама с собой и цепь интерфейс→интерфейс отсекаются.
  Общая база `TileTransvector` готова под дислокатор.
- **1.0.49 — Трансвекторный дислокатор** ✅
  Радиус 16, срабатывание по фронту редстоуна, откат 10 тиков с запоминанием
  импульса. Переносит тайлы (NBT + пересоздание с новыми координатами) и
  существ из обеих точек. Не двигает узлы, `portableHoleBlackList` и
  небьющиеся блоки; воздух двигать можно.

### Фаза 3 — автоматизация и зачарование ✅ завершена

- **1.0.50 — Планшет динамизма** ✅ Режимы удар/применение, замах 45/3 как в
  оригинале, прогрессивная ломка по `getDigSpeed` с ванильной сеткой трещин,
  подтягивание дропов. Вместо GUI — прямое взаимодействие (ПКМ/sneak-ПКМ/
  sneak-ПКМ с редстоуном). Открытый хвост: **нет TESR замаха** — инструмент
  не виден и не качается.
- **1.0.51 — Осмотический чарователь** ✅ Многоблок из 6 столбов
  (2–12 обсидиановых тотемов + нитор, радиус 4) воспроизведён 1-в-1, таблица
  стоимости портирована из `EnchantmentManager` вместе с экспоненциальной
  кривой. Оплата — по 1 первичному вис за тик из жезла.
  GUI-пикер оригинала заменён зачарованной книгой (см. ниже).

### Фаза 4 — прочие блоки ← следующая

Платформа (эфемерная), Редстоун-установщик (`RPlacer`), Камуфляж (`Camo`),
Анализатор аспектов, Силовое поле, газовые блоки (свет/тень/нитор).

### Фаза 5 — предметы

Шлем прозрения, Ткань заклинаний, Чернильница, Настоянные семена/зерно,
Аспекты мобов, Яркий нитор, Форма душ, Книга обмена.

### Фаза 6 — своя ветка исследований

Собственная вкладка/ветка TT в Таумономиконе вместо гейта за чужими
исследованиями. Делать в конце, когда состав контента устоится.

---

## Реестр расхождений с оригиналом (закрыть всё)

**Правило модуля: 1-в-1 с оригиналом.** Никаких упрощений «потому что сложно»
или «потому что нужна инфраструктура». Если в оригинале был экран — делаем
экран; если был рендер — делаем рендер; если была фича — портируем фичу.
Ниже полный список того, чем уже сделанное отличается от TT. Список ведётся
до нуля.

### Закрыто
- ~~**Чарователь без экрана выбора зачарований**~~ — 1.0.53: книжный путь
  удалён, есть `ContainerEnchanter` + `GuiEnchanter` на текстуре оригинала.
  Раскладка по оригиналу: кнопка старта (151, 33), сетка предложений в две
  строки по восемь от (34, 54), очередь строками за правым краем с кнопками
  уровня, шесть полос по первичным аспектам с тултипом «уплачено/всего».
  Предложения фильтруются как в оригинале: зачаровываемость предмета, отсутствие
  уже наложенных чар и совместимость с очередью. Иконки зачарований (29 шт.)
  перенесены из ассетов TT.
- ~~**Планшет динамизма без GUI**~~ — 1.0.52: `ContainerAnimationTablet` +
  `GuiAnimationTablet` на текстуре оригинала, координаты слота и кнопок как в
  `GuiAnimationTablet` TT, кнопки через `enchantItem`/`sendEnchantPacket`,
  клик жезлом вращает планшет.

### Открыто — по убыванию объёма

1. ~~**Нет 14 своих зачарований TT.**~~ — закрыто в 1.0.55: перенесены все
   четырнадцать (`EnchantmentTinkerer` + `ModEnchantmentsTinkerer` +
   `TinkererEnchantmentHandler`), формулы, уровни, слоты и правила
   совместимости оригинала, стоимости в чарователе, названия en+ru, guard.

   Было: В оригинале `ModEnchantments` регистрирует:
   ascent boost, slow fall, auto smelt, desintegrate, quick draw, vampirism,
   dispersed strikes, final strike, focused strikes, pounce, shatter,
   shockwave, tunnel, valiance. Плюс их стоимости в таблице чарователя и гейт
   по исследованиям. Пока в `EnchantmentCosts` есть только ванильные и
   таумкрафтовые — своих TT нет вообще.
2. ~~**Магнит: нет варианта «магнит мобов».**~~ — закрыто в 1.1.1.0: вариант
   живёт во втором бите метаданных как в оригинале, тянет живых существ (не
   игроков), переключатель «взрослые/детёныши», фильтр по форме душ
   (`ItemSoulMould` тоже портирован), свой экран `GuiMobMagnet` на текстуре
   оригинала. `TileMagnet` обобщён хуками вместо `getEntitySelector()`.
10. **Магниты рисуются кубами, а в оригинале — Techne-модели.** И предметный,
    и мобий магнит в оригинале имеют собственную модель с полюсами
    (`magnetN/S.png`, `mobMagnetN/S.png` — развёртки модели, не тайлы блока).
    У нас обе — `cube_bottom_top` с текстурой, вырезанной из развёртки, и
    самодельным маркером режима на верхней грани. Нужны настоящие модели.
    То же касается ремонтника (`repairer.png` — тоже развёртка модели) и
    планшета.

11. **Кнопки в GUI нарисованы прямоугольниками, а не спрайтами оригинала.**
    В TT каждая кнопка — свой класс со своей текстурой (`GuiButtonAT`,
    `GuiButtonATRadio`, `GuiButtonMM`, `GuiButtonMMRadio`,
    `GuiButtonEnchantment`, `GuiButtonEnchanterLevel`, `GuiButtonEnchant`,
    `GuiButtonFramedEnchantment`). У нас — `drawRect`-заглушки в трёх экранах
    (планшет, магнит мобов) и штатные `GuiButton` с «-»/«+»/«>» в чарователе.
    Спрайты кнопок лежат в атласах GUI оригинала — нужно вырезать и рисовать
    как в TT.

12. **Рецепты и исследования всего модуля — свои, а не оригинала.** Пункт 5
    фиксирует это для фокусов, но то же верно для всех остальных предметов и
    блоков TT: у нас аркан-рецепты собственного сочинения и гейт за
    существующими исследованиями TC4, тогда как в оригинале у каждого своя
    запись в ветке TT (`KEY_*`), свои родители, координаты и тип рецепта
    (аркан/инфузия/тигель). Закрывается вместе с Фазой 6 (своя ветка
    исследований).

3. **Трансвекторный интерфейс: нет камуфляжа.** В оригинале тайл наследует
   `TileCamo` и блок рисуется как тот блок, к которому привязан (поля
   `camo`/`camoMeta`). У нас — своя текстура. Нужен baked-model или TESR,
   берущий состояние связанного блока.
4. **Планшет динамизма: нет TESR замаха.** В оригинале инструмент лежит на
   планшете и качается при ударе; у нас блок статичен, механика при этом полная.


### Найдено ревизией 1.0.42–1.0.45 (эти заходы делались без исходника TT)

Работы 1.0.42–1.0.45 (фокусы, утилиты, воронка, тёмный кварц) писались по
названиям текстур и общим представлениям, без сверки с кодом оригинала.
Сверка с `Thaumic-Tinkerer/ThaumicTinkerer@1.7.10` показала, что почти всё в
них расходится. По правилу 1-в-1 это долг, а не «вариант реализации».

5. **Фокусы: поведение и стоимости исправлены в 1.0.54, рецепты и
   исследования — нет.** Закрыто: Smelt, Telekinesis, Heal и Deflect стали
   канальными (`onUsingFocusTick`, оплата за тик), у всех семи стоимости
   оригинала аспект в аспект, Deflect снова щит от снарядов
   (`protectFromProjectiles`), Dislocation снова поднимает блок вместе с его
   тайл-энтити и ставит его в другом месте с тремя ценовыми ступенями, Smelt
   считает прогресс `20 - min(3,потенция)*5` и плавит только блок→блок, Heal
   лечит полсердца на такте `30 - потенция*10/3`, Flight использует импульс
   `1/1.5 * (1 + потенция*0.2)` и сбрасывает счётчик полёта.
   **Осталось открытым:** рецепты и исследования. В оригинале Smelt и
   EnderChest — аркан-рецепты из конкретных компонентов (фокус огня + нитор +
   фокус раскопок; зеркало + око эндера + фокус портативной дыры), остальные
   пять — **инфузия** со своей нестабильностью, и у каждого своя запись
   `KEY_FOCUS_*` в ветке TT с родителями и координатами. У нас — одинаковая
   рамка 3×3 из осколков и кварца и гейт за чужими исследованиями TC4.

   Было до 1.0.54 (для истории):

   | Фокус | Оригинал | У нас сейчас |
   |---|---|---|
   | Smelt | канал (`isUseItem` + `onUsingFocusTick`), FIRE 45 + ENTROPY 12 за тик, счётчик прогресса на конкретный блок, плавит только блок→блок (`result instanceof ItemBlock`) | мгновенный ПКМ, FIRE 30, кулдаун 200, вдобавок дропает предмет для не-блоков |
   | Telekinesis | канал, AIR 5 + ENTROPY 5 за тик | мгновенный ПКМ, MOTION 8, тянет предметы и орбы опыта |
   | Flight | ПКМ, AIR 15 | ПКМ, FLIGHT 10 |
   | Heal | канал, EARTH 45 + WATER 45 за тик, проверка `shouldHeal()` | мгновенный, HEAL 12, +4 hp |
   | Deflect | канал, ORDER 8 + AIR 4 за тик, **щит от снарядов** (`protectFromProjectiles`) | мгновенная ударная волна с отбросом мобов, PROTECT 12 + MOTION 6 |
   | Dislocation | ПКМ, **поднимает блок вместе с его тайл-энтити (NBT) и ставит в другом месте**, ENTROPY 500 + ORDER 500 + EARTH 100; ×5 для блока с тайлом, ×20 для спавнера | телепорт игрока на блок под прицелом, TRAVEL 25 + ENTROPY 5 |
   | EnderChest | ПКМ, ENTROPY 100 + ORDER 100 | ПКМ, VOID 10 + TRAVEL 5 |

   Плюс рецепты и исследования: в оригинале Smelt и EnderChest — аркан-рецепты
   из конкретных компонентов (фокус огня + нитор + фокус раскопок; зеркало +
   око эндера + фокус портативной дыры), остальные пять — **инфузия** со своей
   нестабильностью, и у каждого своя запись `KEY_FOCUS_*` в ветке TT с
   родителями и координатами в Таумономиконе. У нас — одинаковая рамка 3×3 из
   осколков и кварца и гейт за чужими исследованиями TC4.

6. **Placement Mirror и Cat Amulet: поведение исправлено в 1.0.59, тир — нет.**
   Закрыто: оба переписаны по исходнику — амулет стал Baubles и перепрошивает
   ИИ мобов (а не гасит падение), зеркало выкладывает квадрат 3×3…11×11 с
   оплатой из инвентаря (а не один блок). **Осталось открытым:** оба в
   оригинале лежат в `item/kami/` и наследуют `ItemKamiBase` — это KAMI-тир со
   своей редкостью и веткой исследований, которой в порту пока нет. Закрыть
   вместе с KAMI.

   Было: В оригинале оба
   лежат в `common/item/kami/` и наследуют `ItemKamiBase` — это KAMI, эндгейм.
   У нас они обычные предметы за обычные исследования.

7. ~~**Талисманы не Baubles.**~~ — закрыто в 1.0.57: оба `implements IBauble`
   (слот амулета), очищения — 100 зарядов, выключатель и такт раз в секунду
   (тушит горение либо снимает один вредный эффект), опыта — поглощение орбов
   в радиусе 3 до 1500 и обмен 10 опыта + бутылка на бутыль опыта.

   Было: `ItemCleansingTalisman` и `ItemXPTalisman` в
   оригинале `implements IBauble` — работают из слота Baubles. XP-талисман к
   тому же сам поглощает орбы опыта (`EntityXPOrb`), имеет предел
   `XP_TALISMAN_MAX_XP` и меняет накопленное на бутыль опыта за
   `XP_TALISMAN_ENCHANTING_BOTTLE_COST`. У нас — обычные предметы в инвентаре,
   ПКМ кладёт весь опыт, sneak-ПКМ забирает.

8. ~~**Воронка — совершенно другое устройство.**~~ — закрыто в 1.0.56:
   переписана по оригиналу (банка в слоте → 1 эссенция за тик → банка за
   хоппером снизу, с фильтром, пределом 64 и пустотными банками),
   `IAspectContainer` + `IItemHandler`, банка кладётся/забирается кликом.

   Было: Оригинальный `TileFunnel`
   реализует `ISidedInventory`, `IAspectContainer`, `IMovableTile`: держит в
   своём слоте **наполненную банку** и по одной единице переливает эссенцию в
   банку под собой через хоппер, сверяясь с фильтром банки и пределом 64 (или
   без предела для пустотной). У нас — пылесос дропнутых предметов в инвентарь
   снизу. Общее только название.

9. ~~**Тёмный кварц без плиты и ступеней.**~~ — закрыто в 1.0.58: добавлены
   `BlockDarkQuartzSlab` (половинная и двойная) и `BlockDarkQuartzStairs`,
   вырезанные из обычного варианта, с твёрдостью оригинала.

   Было: В оригинале есть
   `BlockDarkQuartzSlab` и `BlockDarkQuartzStairs` со своими ItemBlock'ами.
   У нас только базовый блок с тремя вариантами.

---

## Пересмотр «вне области»

Раньше здесь были пункты, отложенные как «слишком большие». По правилу 1-в-1
они возвращены в план как работа, а не как исключения:

- **KAMI** — эндгейм-тир TT. **Начат в 1.1.2.0:** ресурсы (8 подтипов), дроп
  осколков и все рецепты ресурсов перенесены точно по оригиналу.
  **1.1.3.0:** детали жезла (набалдашник + стержень) и четыре ихориевых
  инструмента на материале `ICHOR` (неразрушаемые, уровень 4).
  Осталось: продвинутые инструменты (режимы и HUD), броня (+крылья),
  варп-гейт (блок, тайл, два экрана, рендер), портал бедрока, три фокуса,
  ихоровый кошель, талисман блоков, небесная жемчужина, протоглина,
  обработчик душевых сердец, ветка исследований KAMI.
- **Своё измерение TT** — портируется как есть; то, что у нас уже есть Далёкие
  земли, не повод его выбрасывать.
- **ComputerCraft-периферия, интеграции AE2 / IC2 / Battlegear2** — в
  оригинале это опциональные интеграции (`@Optional.Interface`), которые
  компилируются только при наличии чужих API. Это не упрощение с нашей стороны,
  а внешняя зависимость: делаем их тогда, когда соответствующий мод есть в
  сборке, тем же опциональным механизмом, что и оригинал.

## Definition of done для каждого захода

1. Класс(ы) в нужном подпакете `tinkerer/` с атрибуцией в javadoc.
2. Регистрация: `ConfigItems`/`ConfigBlocks` (+ItemBlock, +`TILE_REGISTRATIONS`
   для тайлов, +item-модели меты в `ClientProxy`).
3. Ассеты: blockstate + модели + текстуры (из ассетов TT) + `en_us.lang`
   **и `ru_ru.lang`**.
4. Рецепт в `ConfigTinkerer` за существующим исследованием.
5. Расширен `ThaumicTinkererFociStaticGuardTest`.
6. `./gradlew test` — зелёный (правило репозитория: 0 падений).
7. Bump версии + запись в `CHANGELOG.md`.
