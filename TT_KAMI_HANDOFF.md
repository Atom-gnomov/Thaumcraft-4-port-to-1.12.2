# Что осталось от Thaumic Tinkerer: разбор KAMI

> **Работа по этому файлу закончена (на 1.1.42.0).** Все девять объектов
> перенесены, включая последний узел — врата, жемчужина и фокус отзыва.
> Файл писался как план **до** написания кода; планом он больше не является.
>
> **Но удалять его нельзя.** Клона оригинала `../tt-original-1.7.10` на машине
> больше нет (см. таблицу источников в
> [`PORTING_HANDOFF.md`](PORTING_HANDOFF.md)), а значит выписанное здесь
> поведение — количество искр, коробка поиска игрока, порядок звуков,
> сообщения в чат — единственная его запись, кроме самого кода порта. Если
> понадобится свериться, сверяться придётся отсюда.

Разбор оригинала, сделанный до написания кода. Всё ниже прочитано в
`../tt-original-1.7.10`, пока он был на месте; точные значения —
в [`TT_OBJECT_REFERENCE.md`](TT_OBJECT_REFERENCE.md), порядок — в
[`TT_PORT_QUEUE.md`](TT_PORT_QUEUE.md).

Правило прежнее и здесь тоже: см. [`PORTING_HANDOFF.md`](PORTING_HANDOFF.md),
раздел «THE RULE: never invent a value».

---

## Цепочка портала — три объекта разом

`BlockWarpGate` → `ItemSkyPearl` → `ItemFocusRecall`. Брать только в этом
порядке: жемчужина настраивается кликом по порталу, а фокус наводится
жемчужиной.

### `BlockWarpGate` + `TileWarpGate` — Небесный портал

- Блок: `Material.rock`, **твёрдость 5.0, сопротивление 2000.0** (как у
  чарователя). ПКМ открывает экран, при поломке высыпает содержимое.
- Тайл — инвентарь на **10 слотов**, каждый принимает только небесную жемчужину
  (`SlotSkyPearl`). Плюс флаг `locked` в NBT: запертый портал не принимает
  входящие телепорты.
- Каждый тик тайл ищет игроков в коробке
  `(x, y+1, z) … (x+1, y+1.5, z+1)` — то есть **стоящих на портале**, — и если
  игрок приседает, открывает ему **второй** экран, список назначений.
- Телепорт: берётся жемчужина из слота, из её NBT — координаты; целевой блок
  должен оказаться `TileWarpGate` и не быть заперт. Иначе в чат уходит
  `ttmisc.noDest` или `ttmisc.noTeleport`.
- Сам перенос: звук `thaumcraft:wand` (1.0 / 1.0), **20 искр** до и **20**
  после, `player.mountEntity(null)` (в 1.12 — `dismountRidingEntity()`),
  `setPlayerLocation(x + 0.5, y + 1.6, z + 0.5, yaw, pitch)`, затем тот же
  звук с шагом 0.1. Флаг `teleportedThisTick` не даёт сработать дважды за тик.
- Рецепт — инфузия 8 на **тропинке путешествий** (`blockCosmeticSolid:2`),
  TRAVEL 64 / ELDRITCH 50 / FLIGHT 50, с трансвекторным дислокатором, ихором,
  двумя ресурсами KAMI, алмазом и пером.

**Два экрана, оба нужны:**

| GUI id в оригинале | Что это |
| --- | --- |
| `GUI_ID_WARP_GATE = 51` | инвентарь портала: 2 ряда по 5 слотов в `(30 + x·25, 27 + y·25)`, инвентарь игрока ниже |
| `GUI_ID_WARP_GATE_DESTINATIONS = 52` | список назначений, открывается приседанием на портале |

В порту KAMI-экраны уже начинаются с 50 (`CommonProxy.GUI_ICHOR_POUCH`), так что
51 и 52 свободны и совпадут с оригиналом.

### `ItemSkyPearl` — Небесная жемчужина

- `setMaxStackSize(1)`, редкость KAMI.
- **Настраивается** только кликом по `BlockWarpGate`, и только если ещё не
  настроена. Пишет в NBT `x`, `y`, `z`, `dim`; признак настроенности —
  `y != -1`. Сброс — приседающий ПКМ, он же ставит `y = -1`.
- Подсказка показывает X/Z (и Y, если не «простой режим»), а также расстояние
  до цели с двумя знаками или красное `ttmisc.differentDim`, если измерение
  другое.
- Светится, когда настроена (`hasEffect`).
- Рецепт — инфузия 6, выход **2 штуки**, TRAVEL 32 / ELDRITCH 32 / FLIGHT 32 /
  AIR 16 на жемчуге Эндера с ихором, эндер-кристаллом, блоком лазурита и алмазом.
  Исследование у неё чужое — `KEY_WARP_GATE`.

### `ItemFocusRecall`

Наводится жемчужиной, поэтому берётся последним. Значения — в каталоге.

---

## Отдельно стоящие

### `ItemIchorSwordAdv` — пробуждённый ихориевый меч

Единственное, чего ему не хватает, — **обработчик сердец душ**: режим 2 даёт
игроку временные сердца сверх обычного здоровья. Три режима в уроне предмета,
как у остальных пробуждённых: `Single`, `Area`, `Soul`. Рецепт — инфузия 15,
AIR 50 / HUNGER 64 / SOUL 32 / WEAPON 32 / ENERGY 32 / ORDER 16 / CRYSTAL 16.
Механику `SoulHeartHandler` нужно прочитать в оригинале целиком: она хранит
сердца в NBT игрока и перерисовывает HUD.

### `ItemFocusShadowbeam`, `ItemFocusXPDrain`

Инфузия 12 каждый, ничем не заблокированы, читать по каталогу.

---

## Две базы, которые перенесены не будут

`ItemModFocus` и `ItemModKamiFocus` — абстрактные родители фокусов TT. Наши семь
фокусов наследуют `ItemFocusBasic` самого порта и работают, так что классы как
таковые не нужны. Они висят в счёте, потому что генератор считает по именам
классов; когда дойдут руки — добавить их в `RENAMES` или в исключения
`scripts/tt_common.py`, но **не** создавать пустые классы ради счётчика.
