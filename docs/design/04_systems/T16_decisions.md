# 04 · T16: лист решений

154 подтверждённые находки, сведённые к **66 решениям** (дубли между
срезами схлопнуты). Формат: проблема — предложение — отметка о внесении.

✅ **Все 66 закрыты.** Подавляющее большинство — внесением правки со
ссылкой на файл и раздел. Три исключения:

- **§46 — находка отклонена:** защитный костюм IC2 (Hazmat, делается из
  резины) существует, ошибочным было только название;
- **§64 и §66 — приняты как осознанные допущения** решением владельца
  («пофиг»): нитор отдаёт 20 HU/t на каждую сторону, а контур
  «Искры → изумруды → нитор» остаётся. Оба записаны с числами, чтобы это
  было решением, а не незамеченной дырой.

Блокеры и пять решений владельца были внесены до составления этого листа
— здесь их нет (см. `T16_audit.md` §5а).

---

## A. Одно большое (тянет за собой десяток мелких)

**1. Правило актов нарушено всей верхушкой T4.** Объекты Акта IV требуют
Пустотный Иридий и Сердце Колосса, помеченные Актом V.
*Предлагаю:* акт = тир, без исключений. Весь T4 → «IV. Резонанс»
(Пустотный Иридий, Нано-Таум, Стол Шаблонов), акт V остаётся за T5,
данжем и вкладкой «Синтез». Сердце Колосса в рецепте иридиевого стержня
при этом остаётся единственным честным исключением — либо убираем его
оттуда, либо переносим стержень в T5.
*✅ Решение владельца: «да надо решить, без исключений».* Внесено: у
`void_iridium`, `nano_thaum_armor`, `template_bench` акт = тир с явной
пометкой; у иридиевых компонентов шапка разведена — наконечник T4/Акт IV,
стержень **T5/Акт V** (§4 карточки), то есть выбран второй вариант
(«переносим стержень в T5»), и исключений не осталось.

**2. Дом ключа исследования — карточка, а не дерево.** В дереве гейтов
`MORTAR`, `QUANT_VOID`, `QUANT_ICHOR`; в карточках `MECHANIST_MORTAR`,
`QUANT_VOID_ARMOR`, `QUANT_ICHOR_ARMOR`.
*Предлагаю:* правило «ключ живёт в карточке», дерево приводим к ней.
*✅ Решение владельца: «правило „ключ живёт в карточке“, дерево приводим к
ней».* Внесено: в `03_progression.md` §3 стоят `MECHANIST_MORTAR`,
`QUANT_VOID_ARMOR`, `QUANT_ICHOR_ARMOR`.

**3. Стоимости исследований выпадают из лестницы** (`research_design` §2.2):
`PROTECTIVE_BAUBLES` 52 при потолке 50, `DIRTY_ALUMENTUM` 30 при 25,
`BAYONET_ARQUEBUS` 36 при 35–50 без четвёртого аспекта, `TECHNOMANCER`
оправдан несуществующим диапазоном «T2–T3».
*Предлагаю:* привести все четыре к лестнице, диапазон «T2–T3» вычеркнуть.
*✅ Решение владельца: «привести к лестнице, диапазон вычеркнуть».*
Внесено: `PROTECTIVE_BAUBLES` 52 → **50**, `DIRTY_ALUMENTUM` 30 → **24**,
`BAYONET_ARQUEBUS` 3 аспекта/36 → **4 аспекта/40**, `TECHNOMANCER`
узаконен новым **лорным исключением** `research_design.md` §2.2 (запись
без предмета считается на тир ниже); там же прямо сказано, что диапазонов
вида «T2–T3» не бывает.

---

## B. Ссылки, имена, реестр

**4.** Сервитор имеет два registry-имени: `unboundtech:servitor` у босса
против `mechanist_servitor` в своей карточке. — *Оставить второе.* ✅ Сделано ранее.

**5.** Глоссарий канона держит `rad_walker`/`rad_spitter`/`ashling` —
сущности, отменённые вместе с переходом на мутацию обычных мобов. —
*Переписать строку: «своих сущностей нет, метка `UTMutated`».* ✅ Сделано ранее.

**6.** `rad_materials` выдаёт дроп с тех же отменённых «Рад-Ходока» и
«Плевуна». — *«0–2 с любого мутировавшего».* ✅ Сделано: `rad_materials.md` §4.1 — «0–2 с любого мутировавшего».

**7.** Карточка Колосса называет родителем `DUNGEON_MECHANIST_RIFT` —
исследование, которого канон нигде не заводит. — *Завести его в
`dungeon_and_boss.md` (тир, complexity, стоимость, условие — вход в данж).* ✅ Сделано ранее: `DUNGEON_MECHANIST_RIFT` заведён в дереве §3.

**8.** `BOSS_FLUX_COLOSSUS` и `DUNGEON_MECHANIST_RIFT` вообще вне дерева
гейтов. — *Внести блок данжа в `03_progression.md` §3.* ✅ Сделано ранее: блок данжа есть в `03_progression.md` §3.

**9.** `NANO_THAUM_ARMOR` нарисован в дереве дважды с несовместимыми
родителями. — *Один набор: добавить `SINGULATOR` третьим родителем в
карточку, дублирующий узел из дерева убрать.* ✅ Сделано: дублирующий узел из `03_progression.md` убран, родители живут в карточке.

**10.** У `ESSENTIA_BURNER` родитель в дереве и в карточке разный. —
*Взять карточкин.* ✅ Сделано: дерево приведено к карточке (`ESSENTIA_BURNER` ← `TEMPERED_THAUMIUM`).

**11.** Сердце Колосса: «три потребителя» в глоссарии и у Дезинтегратора
против «четыре» в системе данжа. — *Везде четыре.* ✅ Сделано ранее: везде четыре.

**12.** Сердце Колосса объявлено в глоссарии с registry-именем, но
карточки у него нет. — *Завести `05_objects/colossus_heart.md`.* ✅ Сделано: заведена `05_objects/colossus_heart.md`; долг карточек в `00_index.md` закрыт.

**13.** Таум-Генератор гейтится исследованием `THAUM_STEEL`, которого нет
ни в каноне, ни в коде; имя «таум-сталь» канон отменил. — *Заменить на
`TEMPERED_THAUMIUM`, в коде сабмода тоже.* ✅ Сделано ранее в доках; в коде сабмода — при переносе.

**14.** `SOLAR_SUNNARIUM` и `TECHNO_MATERIALS` зарегистрированы в коде, но
дерева гейтов не имеют. — *Дописать в дерево.* ✅ Сделано ранее: обе записи есть в дереве §3.

**15.** `TECHNOMANCER` объявляет родителем корневой `THAUM_IC2_INTRO` (T1),
хотя сам T3. — *Перевесить на T3-родителя.* ✅ Сделано: родитель `TECHNO_SPIRITS` (T3); диапазон «T2–T3» вычеркнут, цена узаконена лорным исключением `research_design.md` §2.2.

**16.** Три карточки ссылаются на `§6.7` за правилом примордиалов, которое
после вставки правила «один на чанк» уехало в `§6.8`. — *Поправить ссылки.* ✅ Сделано ранее: все три ссылки ведут на §6.8.

**17.** Карточки ссылаются на «§2.4», «§2.4а», «§2.6 канона» — таких
разделов в `02_world_model` нет, они в `aspect_economy`. — *Дописать имя
файла, убрать слово «канона».* ✅ Сделано: во всех карточках «§2.x канона» → «`aspect_economy.md` §2.x».

**18.** Строка массфабрикатора помечена 🔜 «спроектировано», хотя ни
системы, ни карточки нет. — *Пометить ❓ или завести карточку.* ✅ Сделано: строка массфабрикатора помечена ❓ «не спроектировано» с явной оговоркой в §3.1.

---

## C. Числа, которые просто посчитаны неверно

**19.** Сингулятор: «полная зарядка ~50 секунд» — по собственным числам
150. И 2 000 EU/t не влезают в заявленный HV (512 EU/t). — *Либо тир EV,
либо скорость 1 вис / 20 тиков; время пересчитать.* ✅ Сделано ранее: тир EV, 150 секунд.

**20.** Кольца Схемы: 100 000 EU при 2 EU/t — это 42 минуты, а не 83. —
*Написать 42; либо расход 1 EU/t, если хочется 83.* ✅ Сделано ранее: 42 минуты.

**21.** Эфирный Двигатель: 8 000 EU при 128 EU/t — 3,1 секунды, а не 2,5. —
*Пересчитать (с округлением до цикла в 20 тиков — 4 сек).* ✅ Сделано ранее: ~4 сек (округление до цикла).

**22.** Патронная Линия: 8 циклов по 8 патронов = 64, а лента объявлена в
60. — *Согласовать.* ✅ Сделано: лента — **750 тиков** (7.5 цикла), 60 патронов, не 64.

**23.** Переработка руд: «цикл убыточен» при собственном подсчёте 6 → 8
(это +33 %), и средняя Фортуны III посчитана как 2.2 вместо 2.5. —
*Признать цикл прибыльным и заново решить вопрос по закону 2.* ✅ Сделано: `ore_processing.md` — Фортуна III даёт **2.5** (`1 + rand(1+fortune)`), а цикл через кластер признан прибыльным (+33 %) и законным как конвертер «кварц + EU → осколки», а не «убыточным».

**24.** Гильза: аспекты посчитаны от обычного таумия, хотя она из
закалённого. — *Пересчитать.* ✅ Сделано ранее: считается от закалённого.

**25.** Таумиевый ключ: аспекты выведены из закалённого таумия, хотя ключ
из обычного; плюс §10 держит вопрос открытым, а §12 объявляет решённым. —
*Пересчитать от обычного, вопрос закрыть.* ✅ Сделано: аспекты от обычного таумия (ранее), а открытый вопрос §10 закрыт — `IWrenchable` публичен и живёт на блоке, ключ разбирает и машины IC2.

**26.** Приёмка закалённого таумия проверяет аспекты, которые эта же
карточка объявила ошибочными. — *Привести к §7.* ✅ Сделано ранее.

**27.** Иридиевый наконечник: аудит зафиксировал решение владельца **0.75**,
карточка держит **0.85**; ёмкость стержня 150 в аудите против 125 в
карточке. — *Нужно твоё слово: какое из двух.* ✅ Решение владельца: наконечник **0.75**, ёмкость стержня **150**. Внесено.

**28.** Порог формулы аспектов: канон задаёт `max(1; 15 %)`, а пример и обе
✅-карточки считают по `max(2; …)`. — *Привести к max(1); наборы аспектов
при этом не меняются.*

**29.** Грязный Алюментум: сумма энергоценности частей 20 000 EU, а выход
60 000 — крафт утраивает энергию из ничего. — *Либо 40 000 (уже назван
кандидатом), либо признать это ценой радиационного фона.* ✅ Решение владельца («крафт утратит энергию» / «пусть будет норм баланс по итогу энерго»): грязный алюментум = **20 000 EU**, ровно сумма частей. Хвосты со старым числом 60 000 вычищены в `rad_materials.md` §9, глоссарии §5 и `radiation.md` §12.7 (пункт закрыт целиком).

---

## D. Утверждения про Thaumcraft, которые не подтверждаются кодом

**30.** «Рунный щит ТК бесплатный и вечный» — в коде каждое очко щита
оплачивается вис из ауры. — *Переписать вывод: наша ниша не «мы платим, а
они нет», а «мы платим электричеством вместо вис».* ✅ Сделано: `baubles_and_foci.md` §1 (строка цены), `protective_baubles.md` §1/§2/§5/§12.

**31.** «Рунный щит не ловит урон от падения» — в коде `FALL` поглощается. —
*Убрать.* ✅ Сделано: `baubles_and_foci.md` §1, `protective_baubles.md` §10 (строка разбита на три).

**32.** «Потолок жезла 150 вис на примал, полный жезл = 900» — в коде
максимум даёт серебряное дерево (100 → 600). — *Назначить эталонный
стержень явно.* ✅ Сделано: `02_world_model.md` §1 и §3.1 — таблица по стержням; 900 закреплено за нашим иридиевым, 600 — за серебряным деревом.

**33.** «Гогглами работает любой предмет брони с `IGoggles`» — движок
читает только шлем. — *Уточнить.* ✅ Сделано: `machine_feedback.md` §6.1 (три места чтения) и §6.2.

**34.** Электрочернильница описывает не тот путь расхода: стол берёт
инструмент из своего слота, а не из руки. — *Переписать механику.* ✅ Сделано: `electric_scribing_tools.md` §4 — оба пути расхода расписаны, трансляция «прочность ↔ EU» перенесена в `setDamage`/`getDamage`, потому что `setItemDamage` минует `damageItem`.

**35.** «Четыре заряженных кольца дают мгновенную перезарядку» — по
формуле выходит не мгновенно. — *Пересчитать.* ✅ Сделано: `baubles_and_foci.md` §1 — колец два, потолок 1 очко/сек, плюс строка про паузу 80 тиков.

**36.** `research_design` называет complexity третьим параметром
конструктора `ResearchItem` — в порте он другой. — *Поправить.* ✅ Сделано: `research_design.md` §2.4 — шестой параметр, зажим 1…3, короткий конструктор + `setComplexity`.

**37.** `aspect_economy` утверждает, что вручную написанные
`Tutamen`/`Telum`/`Perfodio` дублируют то, что ТК добавит сам — на деле
не добавит. — *Проверить и переписать.* ✅ Сделано: `aspect_economy.md` §2.6 переписан (merge = максимум; движок смотрит на класс предмета). Следствие разнесено по карточкам: у шести оружейных `Telum` помечен как назначенный вручную, у Патронной Линии заменён на `Fabrico`.

**38.** Рецепт Резонансного Амулета называет заготовку «золотым амулетом»,
в порте она называется иначе. — *Поправить имя.* ✅ Сделано: `protective_baubles.md` §6 — «Обычный амулет» (мета 0) и «Обычный пояс» (мета 2).

**39.** Шинный узел объявлен работающим «как трансвекторы ТК» — те
работают не так. — *Переписать обоснование.* ✅ Сделано: `bus_node.md` §4.1 — прецедент заменён на эссенциальные трубы (`IEssentiaTransport`).

**40.** §1.1 канона говорит, что TC6-фасад ауры «ничего не читает и не
пишет» — частично неверно. — *Уточнить формулировку.* ✅ Сделано: `02_world_model.md` §1.1 — фасад читает и пишет, но в статическую карту без сохранения, регенерации и потребителей.

**41.** Индукционный Тигель: утверждение про примордиальную страницу
неверно. — *Проверить и поправить.* ✅ Сделано: `induction_crucible.md` §4.1 — примордиал теряется всегда, флюкс лишь с шансом 1/4.

---

## E. Утверждения про IC2 и аддоны, которые не подтверждаются

**42.** Тиры зарядки брони: заявлены MV и HV «как у нано/кванта», в IC2 это
**HV и EV**. — *Поправить обе карточки.* (у Нано-Таума уже сделано) ✅ Сделано: HV у Нано-Таума, EV у кванта.

**43.** Аспектный Синтезатор: фиксированные 40 тиков цикла несовместимы с
ценой 20 000 EU × N и собственным приёмом. — *Сделать длительность
производной от цены.* ✅ Сделано: длительность цикла = **40 тиков × N прималов**, поток ровно 500 EU/t на любом аспекте — в потолок EV больше не упирается.

**44.** Канон ссылается на несуществующие элементы API: метод
`getWrenchDrop`, «шанс потери» через `getWrenchDropRate`. — *Убрать.* ✅ Сделано: `machine_feedback.md` §5 — `getWrenchDrops` с реальной сигнатурой, `getWrenchDropRate` убран как остаток старых версий IC2.

**45.** «Солнечные панели IC2 и ASP не работают в дождь» — неверно для
обоих. — *Убрать это следствие из системы радиации.* ✅ Сделано ранее.

**46.** Сервитор награждает за «полный резиновый комплект IC2», а там есть
только резиновые ботинки. — ❌ **Находка отклонена владельцем.** Комплект
существует, ошибочным было только название: это **защитный костюм**
(Hazmat Suit), который *делается из резины*, — шлем, куртка, штаны и
резиновые ботинки. Награда за полный комплект (урон током вдвое меньше)
остаётся; в карточке уточнены registry-имена и проверка через
`hasCompleteHazmat` (`mechanist_servitor.md` §4.1).

**47.** `T13_crosscheck` фиксирует как факт «расход воздуха домны — 1 мБ за
цикл», хотя за тик. — *Поправить (частично уже сделано).* ✅ Сделано: `T13_crosscheck.md` П-5 — `fluid` списывается **за тик**, при `duration 6 000` это 6 000 мБ на рецепт.

**48.** Названные примеры рецептов для преобразователя ASP («уголь →
алюментум») в его API невыразимы так, как описано. — *Переписать §3.2
`ranged_weapons`.* ✅ Сделано: `ranged_weapons.md` §3.2 — у преобразователя ASP один вход и один выход, катализатор невыразим. Гейт перенесён на вход: наш предмет-шихта крафтится на арканном верстаке с Матрицей, преобразователь работает с шихтой. `cartridges.md` §6 приведена к этому.

**49.** В `T15_audit` П-1 утверждение про ASP-шлем неверно: он возвращает
не то, что я написал. — *Поправить сам аудит.* ✅ Сделано: `T15_audit.md` П-1 — у ASP-шлема `getArmorDisplay` = `round(3 × ratio)` при заряде и 0 на нуле. Приём взят в обе наши брони: полоска показывает защиту при заряде и падает в 0 на разряженной.

**50.** «Свет 7 — как у работающего генератора IC2»: блоки IC2 не светятся. —
*Убрать сравнение, число оставить.* ✅ Сделано: `machine_feedback.md` §4 — сравнение убрано (машины IC2 света не излучают), число 7 оставлено с собственным обоснованием.

**51.** Гогглы обещают читать буфер чужих блоков IC2 через
`getEnergyStorage` — такого контракта нет. — *Убрать или заменить.* ✅ Сделано: `machine_feedback.md` §6.2 — `getEnergyStored` живёт только на `ic2.api.energy.prefab.*`. Напряжение читаем у всех, запас и поток — только у своих; для чужих гогглы честно пишут «недоступен».

**52.** «У IC2 есть лук с термострелами» — нет. — *Убрать из сверки оружия.* ✅ Сделано: `ranged_weapons.md` §2 — у IC2 дальнобойного оружия нет вовсе, строка про «лук с термострелами» убрана.

**53.** Три карточки просят звуки `ic2:machines.extractor_op` и
`compressor_op` — надо проверить, существуют ли они. — *Сверить с jar.* ✅ Сверено по jar 2.8.195: файлы `Machines/CompressorOp.ogg` и `Machines/ExtractorOp.ogg` есть, но это **пути внутри jar** (`getStartSoundFile()` + `IC2.audioManager`), а не `SoundEvent`-ы — через `world.playSound` их не позвать. Решение: свои .ogg и свой `SoundEvent` по образцу; правка внесена в три карточки.

**54.** Вопрос «есть ли у IC2 контракт „я ключ“» одновременно открыт в
одном файле и закрыт в другом. — *Закрыть: `IWrenchable` есть.* ✅ Сделано: `machine_feedback.md` §5.1 и §7.2 закрыты, `thaumium_wrench.md` §10 тоже — `IWrenchable` публичен и живёт на блоке.

---

## F. Баланс

**55.** Броня из закалённого таумия **не хуже алмазной ни при каком
уроне** — то есть строго лучше без платы. — *Нужна плата или срез числа.* ✅ Сделано: `tempered_thaumium_armor.md` §5.2 — ложное обоснование «по крупным ударам хуже» вычеркнуто с расчётом по ванильной формуле; плата остаётся та, что назвал владелец (исследование, время, потолок зачарований).

**56.** Инструменты из закалённого таумия — **строго лучший алмаз** без
единой платы, вопреки собственному принципу. — *То же.*
(Здесь может быть твоё «так и задумано» — тогда просто запишем.) ✅ Сделано ранее (`tempered_thaumium_tools.md` §5.1а — «это не дефект»). В этот заход поправлена только съехавшая строка таблицы.

**57.** Обоснование §3.2 бижутерии ложно: связка «наш амулет + наш пояс +
два рунных кольца ТК» даёт 36 щита против заявленного потолка ТК 28. —
*Пересчитать вывод.* ✅ Сделано: `baubles_and_foci.md` §3.2 и `protective_baubles.md` §4.3. Признано, что реальный потолок — 36 (смешанный сет). Числа не режем, вводим правило EU-щита: без EU наш вклад = 0, и каждое очко общего пула стоит 1 000 EU.

**58.** «Стационарной артиллерии нет ни у кого» — у METS две лазерные
башни, обе мощнее нашей Мортиры. — *Переписать нишу Мортиры.* ✅ Решение владельца: «сделаем её по площади и красивее визуал». Внесено: радиус поражения 4 блока с линейным спадом 24 → 8, ниша переписана.

**59.** Трофейная Аркебуза Механистов доминирует крафтовую. — *Либо срезать
трофей, либо дать крафтовой то, чего нет у трофея.* ✅ Решение владельца: «согласен». Внесено: трофейной Аркебузе — 4096 выстрелов без починки и апгрейдов, крафтовой — починка и апгрейды.

**60.** Убывающая отдача щупальца не распространяется на **опыт**, а опыт в
этой сборке конвертируется в ресурсы. — *Резать и опыт.* ✅ Сделано: `radiation.md` §6 и `rad_tentacle.md` §4.2/§5/§11 — опыт режется тем же коэффициентом, с обоснованием через курс METS (~160 EU за очко).

**61.** Критерий цели Мортиры («враждебный моб», «не по мирным») в 1.12
выражается хуже, чем написано. — *Уточнить.* ✅ Сделано: `mechanist_mortar.md` §4.1 — три флага и радиус по образцу OMT `TargetingSettings`.

**62.** ЭМИ-волна Колосса забирает EU «себе в щит», но эффект накопленного
нигде не описан. — *Дописать или убрать.* ✅ Сделано: `flux_colossus.md` §4.2а — поглощённое EU становится временным щитом (5 000 EU = 1 очко, потолок 60, распад 2/сек) плюс три пункта приёмки.

**63.** Фаза 3 боя обещает «выбирайся и заходи снова», а пьедестал не
примет Искры ещё 7 игровых дней. — *Согласовать.* ✅ Сделано: `flux_colossus.md` §4.2б — Искры тратятся в момент **смерти** Колосса. Поражение не запускает кулдаун: зайти можно сразу. Кулдаун 7 дней гейтит награду, а не попытку.

**64.** Нитор в реализации отдаёт 20 HU/t **на каждую сторону** (до 120 HU/t
≈ 60 EU/t с блока), а канон обещает 20. — *Важно: это меняет экономику
нитора вшестеро. Нужно твоё решение — правим канон или порт.* ✅ Решение владельца: «пофиг, пусть так будет — в IC2 тепловая энергия это дно». Канон приведён к реализации (`02_world_model.md` §3.2: 20 HU/t на сторону, до 120 HU/t ≈ 60 EU/t).

**65.** Искровая лента непроизводима: Линия требует 60 Заряженных Искр в
слоте, а Искра не стакается больше 8. — *Либо стак Искр, либо другой
рецепт ленты.* ✅ Решение владельца: «лента требует 5, одна на 12 патронов». Внесено в `cartridges.md` §4.3 и `cartridge_line.md` §4.1.

**66.** Нитор продаётся техномагом за изумруды, а изумруды бесконечно
фармятся с наших же техно-духов. — *Убрать нитор из торговли либо изумруды
из дропа.* ✅ Решение владельца: «пофиг». Контур не закрываем, но записан с числами в `technomancer_villager.md` §5.1 — вместе с естественными ограничителями и указанием, что чинить в случае чего надо вечность нитора, а не торговлю.